# 接力文档（心之音）

> 目的：记录当前进度、分工约定、部署方式、排查流程。每轮大改动后更新本文档。
> 最后更新：2026-09-22

## 分工约定（重要）

- **iOS / Android 编译由操作者本人执行**，AI 不再跑客户端编译。
- AI 负责：改代码、后端/Web/管理端的构建与部署、写文档。
- 客户端编译命令备查：
  - Android：Android Studio 绿色运行按钮（debug 变体已配置 `isDebuggable = false`，性能等同 Release；需要断点调试时把 `app/build.gradle.kts` 里 debug 块的 `isDebuggable` 临时改回 `true` 再 Sync）。
  - iOS：Xcode 直接 Run；命令行验证 `xcodebuild -project PeiwanIos.xcodeproj -scheme PeiwanIos -destination 'generic/platform=iOS' -configuration Debug CODE_SIGNING_ALLOWED=NO build`。

## 代码仓库

- GitHub：`https://github.com/yinyuan1990/aiheartchat.git`（分支 main，凭据走 osxkeychain）。
- **注意：仓库是 Public**，`.gitignore` 已排除 `houduan/.env`（AI key）与 `deploy/app/docker-compose.yml`（JWT/数据库密码）——这两个文件只存在于本机和服务器，换机器要单独拷贝。建议把仓库改成 Private。

## 服务器与部署

- 应用服务器 `8.162.5.160`（root，密码见本地凭据，不写入文档）：
  - 代码目录 `/opt/peiwan/houduan`，部署目录 `/opt/peiwan/deploy/app`（docker compose）。
  - 后端部署：scp 改动的 `houduan/src/**` 与 `prisma/schema.prisma` 到服务器对应目录 → `cd /opt/peiwan/deploy/app && docker compose build api && docker compose up -d api`。
  - **schema 变更无需手动迁移**：api 容器启动命令自带 `npx prisma db push`。
  - Web 部署：本地 `web/ npm run build` → scp `web/dist/.` 到 `/opt/peiwan/deploy/app/www/web/`。
  - 管理端部署：本地 `admin/ npm run build` → scp `admin/dist/.` 到 `/opt/peiwan/deploy/app/www/admin/`。
  - 健康检查：`curl http://localhost:20080/api/gifts` 应返回 200。
- SRS 流媒体服务器 `47.122.115.33`（独立，已就绪，一般不动）。

## 当前正在排查的问题：视频通话概率性无画面

已搭好完整的日志上报链路，等待复现后取日志分析：

1. **客户端上报**：两端 `CallManager` 的 `clog()` 在本地打印之余，把日志（带毫秒时间戳）缓冲、每 3 秒批量 POST `/call/log`（`{callId, platform, lines[]}`），挂断时立即冲刷。callId 在发起/收到邀请时绑定（`bindLogCall`）。
2. **后端**：表 `call_client_log`（`CallClientLog`），接口 `POST /call/log`（仅通话参与方可写，单批 ≤500 行）。
3. **管理后台 → 「通话日志」模块**：通话记录列表（双方、状态、时长、日志批数）→ 查看 → **下载日志**（双端合并为一个 txt）。

**操作流程**：用两台手机复现一次"无画面"的视频通话 → 管理后台 → 通话日志 → 找到那通电话 → 下载 txt → 发给 AI 分析。日志里重点看 WHIP/WHEP exchange 结果、onTrack 是否触发、renderer 绑定。

**根因已定位**（2026-08-15，通过帧级统计日志对比 `dca71899…` 一通）：
- 现象：Android 拉流 `bytesRecv=0`（音视频都是 0），但 ICE CONNECTED、onTrack 也触发；iOS 推流 framesEncoded 正常增长。
- 原因：**SRS 接受"发布者尚未推流"时的 WHEP 订阅**（返回合法 SDP、ICE 能通），但发布者随后才推流时，这个早到的订阅者永远收不到 RTP。双方 accept 后并行建流，谁的订阅先于对方的推流完成，谁就黑屏/无声——这就是"概率性"的来源（Android 建流通常比 iOS 快，iOS 要等前台+音频会话+摄像头）。
- 修复（主方案，信令保证时序）：新增 `POST /call/:callId/published` + WS 事件 `published`——每端 WHIP 推流成功后通知对方，对方**收到 published 才开始 WHEP 订阅**（10 秒超时兜底直接订阅）。后端已部署。
- 修复（兜底）：两端 `CallManager` 保留**拉流零流量看门狗**——订阅完成 4 秒后 inbound-rtp 仍 0 字节则重新 WHEP 订阅（最多 4 次），日志 `pull NO media, resubscribe #n` / `pull media flowing`。**需重编两端验证**。

帧级日志（保留，排查任何通话问题都有用）：
- 每 5 秒 RTP 统计：push 端 framesEncoded/bytesSent，pull 端 framesReceived/framesDecoded/bytesReceived。
- 摄像头采集：iOS `capture started WxH@fps`（fps 已按格式上限收敛）；Android CameraEventsHandler（打开/出首帧/错误/冻结）。
- 渲染器首帧：两端 `renderer[remote/local]` 首帧与分辨率回调。

## 视频计费新体系（2026-08-15 上线）

- **成本价 = 流量费**：640x480@800kbps + 音频 ≈ 6.4MB/分钟/路，一通视频 SRS 出流量 2 路 ≈ 14.7MB/分钟 ≈ 0.012 元/分钟。`videoBaseFenPerMin` 现为流量成本价，已设 **2 分/分钟**（留余量）。
- **平台倍率 `videoPlatformX`（默认 2）**：平台每分钟抽成 = 成本 × 倍率（即平台赚 1 倍流量费）；女方收入 = 价格 − 抽成。
- **女生定价规则**：自定价必须**高于**抽成（后端 `PUT /user/me` 校验），三端编辑资料实时显示手续费与到手收入；未自定价默认 = 成本 ×5。
- **已重置全平台女生价格 = 成本 ×5 = 10 分/分钟（0.1 积分）**，共 4 人。管理后台「通话/计费」页可调成本价与倍率，并有「重置全平台女生价格」按钮。
- 注意：之前后台把"视频通话默认价"设成过 10 积分，本次已按新语义覆盖为 2 分成本价；若嫌价格太低，直接在后台调大成本价或倍率即可（女生价格下限、默认价、平台抽成全部联动）。

## 主页「遇见」+ 亲密度系统（2026-08-15 上线）

- **主页两大板块：「动态」+「遇见」**（第二轮调整）：原推荐+同城合并为「动态」（全国动态流），原「关注」tab 移除、入口改为「我的 → 关注动态」独立页；「视频」（抖音模式）入口只在动态板块显示，遇见板块没有。
- **「遇见」板块**：子栏 所有 / 新人 / 同城 / 亲密度，两列大图卡片流（已认证/新人徽章、在线点、★评分、城市、视频价格按钮三态；亲密度栏右下显示 ♥ 分数）。三端已实现：
  - Android `ui/screen/MeetScreen.kt`（PlazaScreen 接入）、iOS `MeetViews.swift`（FeedViews 接入）、Web `Plaza.tsx` 内 `MeetSection`。
- **亲密度记分规则**：任何互动，发起方 +1 分、接受方 +0.5 分——单聊消息每条、视频通话每分钟（主叫+1/被叫+0.5，不足 1 分钟按 1 分钟）、点赞、评论（取消点赞不扣分）。
- **后端**：表 `intimacy`（有向对 user_id→peer_id，score 存 x10 整数半分粒度）；全局 `IntimacyService.bump()` 挂钩在 `im.service` 发消息、`call.service` 挂断结算、`moment.service` 点赞/评论。接口 `GET /user/meet/list?tab=all|new|city|intimacy`（city 可带 `&city=名`），性别隔离只出异性，含在线/占线/视频实际价/isNew(7 天)/intimacy 字段。
- **历史数据已回填**（消息/视频分钟/点赞/评论一次性汇总进 intimacy 表），已在服务器验证亲密度排序正确。
- 注：礼物赠送暂未计分（用户只点名了消息/视频/点赞/评论），需要的话在 gift.service 加一行 bump 即可。

## 近期已完成（本轮）

- **表情包：Telegram 公开贴纸集接入聊天 + 评论（2026-09-20）**。后台精选贴纸集（`t.me/addstickers/xxx`），通过已登录的 Telegram 账号整包拉下来转存 MinIO，三端聊天和动态/树洞评论都能发。
  - **格式归一化（服务端做，三端不用各自处理 Telegram 的三种格式）**：静态 `image/webp` 原样；动态 TGS（gzip 的 Lottie）解压成 Lottie JSON（`format=lottie`，三端用 Lottie 库播）；视频贴纸 WebM(VP9+alpha) 用 **ffmpeg 转成带透明的动态 WebP**（`format=awebp`，AVPlayer 不认 WebM、ExoPlayer/WebView 不画 alpha）。Dockerfile 运行镜像加了 `ffmpeg`（镜像大 ~250MB）。ffmpeg 命令在 `sticker.service.ts webmToAnimatedWebp()`（必须 `-c:v libvpx-vp9` 解码才保留 alpha，320px/20fps/q72）。
  - **后端 `src/sticker/`**：表 `sticker_set`（shortName 唯一、title/kind/count/thumb/enabled/sort/importedCount/lastError）、`sticker`（setId+tgDocId 唯一、emoji/format/url/thumb/w/h/size/sort）；`sys_setting.sticker_version` 任何变更 +1，客户端按版本缓存整套清单。同步走队列（`enqueue → pump`），每张下载 + 转码 + 转存，4 路并行；重新同步只补缺的。`GetStickerSet` 拒绝 masks / emojis 类型（面具 / Premium 自定义 emoji）。
  - **贴纸载荷 `StickerPayload` `{id,format,url,thumb,w,h,emoji}`**：聊天消息 `type=sticker` 的 `content` 就是这个 JSON（自包含，接收方不回查，包下架后旧消息仍能显示）；`moment_comment` / `treehole_comment` 各加 `sticker VarChar(600)` 列存同样的 JSON，评论接口接受 `stickerId`（文字可空）。**sticker 已加进 `CHARGED_TYPES`**（男→女按条扣费，与文字一致）。
  - 接口：用户端 `GET /stickers?ver=` → `{version, notModified, sets:[{id,title,kind,thumb,items:[payload]}]}`。后台（AdminGuard）`GET /admin/stickers/sets`、`GET /admin/stickers/sets/:id/items`、`GET /admin/stickers/preview?name=`（解析 + 前 12 张缩略图 data URL，不入库）、`GET /admin/stickers/featured`（Telegram 官方热门榜，缓存 10 分钟）、`POST /admin/stickers/sets {name}`（添加并开始同步）、`POST /admin/stickers/sets/:id/sync`、`PUT /admin/stickers/sets/:id {title,enabled,sort}`、`PUT /admin/stickers/reorder {ids}`、`DELETE /admin/stickers/sets/:id[?purge=1]`（默认只删记录保留文件，已发出的表情不裂图；purge 连文件删）。最多 40 个包。
  - **踩坑：不要用 `messages.SearchStickerSets`**——线上实测其返回里有 GramJS 2.26 不认识的新 TL 构造器，反序列化报 `readUInt32LE` 错误后整个 MTProto 接收循环卡死，之后所有 Telegram 请求都超时（只能重启 api）。已移除搜索功能；`GetStickerSet` / `GetFeaturedStickers` 正常。另加了保险：`StickerService.guarded()` 给 Telegram 请求套 40 秒超时，超时调 `TelegramClientService.recover()` 强制重连（登录态不丢）。
  - **管理后台**：「Telegram 来源」新增第四个分区「表情包」（`views/tg/TgStickerSets.vue`）：粘贴链接解析 / 热门榜 一键添加；已收列表可 ↑↓ 排序（= 面板 tab 顺序）、查看、改名、停用、重新同步、下架、彻底删除。**操作者待办：登录后台加几个包**（热门榜里挑，视频类也可以）。
  - **三端统一交互**：输入栏「☺」按钮 → 面板顶替键盘：顶部横向 tab（emoji 插文字 / 🕒最近 / 各包封面），下面 5 列网格显示静态缩略图。**聊天里点贴纸即发送**（Telegram 式）；**评论里点贴纸挂到输入栏上方（小图 + ×），再点发送**，文字可同时带。「最近使用」存客户端本地（24 个）。消息气泡里贴纸 140dp 不画气泡底、循环播；评论里 96dp。会话列表 / 通知预览「[表情]」。
  - Web/H5：`web/src/stickers.ts`（目录缓存 localStorage + 最近使用 + `useStickers()`）、`components/StickerPanel.tsx`、`components/StickerView.tsx`（Lottie 用 `lottie-web` light 版**按需动态 import**，进视口才播；动态 WebP 直接 `<img>`）。改 `ChatRoom.tsx`、`ChatList.tsx`、`MomentDetail.tsx`（原 EMOJIS 面板升级为统一面板）、`Treehole.tsx` 详情评论。
  - Android：`ui/sticker/Stickers.kt`（`StickerStore` SharedPreferences 缓存、`StickerImage`/`StickerThumb`/`StickerPanel`/`SmileIcon`）；依赖新增 `com.airbnb.android:lottie-compose:6.7.1` 与 `io.coil-kt:coil-gif`，`MainActivity` 的全局 ImageLoader 加了 `ImageDecoderDecoder`（API 28+ 播动态 WebP）。改 `ChatScreens.kt`（输入栏笑脸按钮 + `Bubble` sticker 分支 + 预览）、`MiscScreens.kt` 动态详情评论（输入栏重做成 BasicTextField + 笑脸）、`TreeholeScreens.kt` 详情评论；`CommentItem`/`TreeholeComment` 加 `sticker` 字段。
  - iOS：`StickerViews.swift`（`StickerStore` UserDefaults 缓存、`StickerImageView`：Lottie 用 **lottie-ios SPM 包（已写进 pbxproj，`https://github.com/airbnb/lottie-ios.git` 4.5+，Xcode 打开会自动解析）**，动态 WebP 用 ImageIO `CGAnimateImageDataWithBlock` 逐帧不依赖第三方；`StickerPanel`、`PendingStickerChip`）。改 `ChatViews.swift`、`FeedViews.swift` 动态详情、`TreeholeViews.swift` 详情、`Models.swift`。
  - **兼容性**：老版本 App 收到 `type=sticker` 会走 default 分支把 JSON 当文字显示；三端需同批重编发版。后端、Web、管理后台已部署。
  - **表情商店（2026-09-20 下午，按操作者反馈改）**：后台加的包不再"所有人自动都有"，改成 Telegram 式——后台维护**库**，用户自己决定面板里有哪些、什么顺序。
    - 表 `user_sticker_set`（userId+setId 唯一，sort）；`user.sticker_inited`（首次拉「我的」时把后台标了 `is_default` 的包写进去，只对没初始化过的用户生效）；`sticker_set.is_default`。
    - 用户接口：`GET /stickers/mine` → `{ids}`（有序）、`POST /stickers/mine/:setId`（新加的排最前）、`DELETE /stickers/mine/:setId`、`PUT /stickers/mine {ids}`（整体排序），都回 `{ids}`。后台：`PUT /admin/stickers/sets/:id` 多了 `isDefault`；`POST /admin/stickers/sets/:id/push-all` 把某包塞进所有用户的面板（追加到末尾，已有的不动）；列表多了 `users`（加了这个包的人数）。删除包时顺带删 `user_sticker_set` 记录；仅「停用」的包 `mine` 返回时过滤掉（重新启用会自动回来）。
    - 三端面板：tab 只显示「我的」包，右侧固定「+」进**表情商店**全屏页（上半「我的表情」置顶 / 移除，下半「全部表情包」添加，每行带前 8 张预览）。「我的」id 列表也本地缓存（Web localStorage `stk_mine` / Android prefs `mine` / iOS UserDefaults `stk_mine`），进面板时向后端刷一次；当前 tab 的包被移除会自动回到「最近」。Web `components/StickerStore.tsx`、Android `StickerStoreScreen`（全屏 Dialog）、iOS `StickerStoreView`（fullScreenCover）。
    - 另两处按反馈改：面板网格和评论待发小图**直接播动画**（`StickerThumb` 现在就是 `StickerImage`/`StickerView`，网格只组合可见行，性能可接受）；目录刷新时把「最近使用」里已不在任何启用包中的贴纸清掉，后台下架后客户端最近列表不残留。
    - 后端、Web、管理后台已部署（schema 已 db push：`user_sticker_set` / `user.sticker_inited` / `sticker_set.is_default` 已在库里）。**Android/iOS 需重编**（本机无 Android SDK，Kotlin 只做了人工检查）。**操作者待办**：后台「表情包」分区把想让新用户默认就有的包勾上「默认」，老用户可点「推给所有人」。
  - **表情面板按 Telegram 重做 + GIF（2026-09-21，操作者给了 9 张 TG 截图）**：面板改成 **GIF / 贴纸 / 表情 三模式**，底部悬浮胶囊切换；三端同一套结构（Web `components/EmojiPanel.tsx` + `components/emoji-panel/{shared,StickerPane,EmojiPane,GifPane}.tsx`、Android `ui/sticker/EmojiPanel.kt`（+ `EmojiData.kt`）、iOS `EmojiPanelViews.swift`）。旧 `StickerPanel` / 全屏商店页已删。
    - **交互细节（三端一致）**：① 内容往下滚 → 底部胶囊 + 顶部条（封面条 + 搜索行）一起收起，往上滚 / 回到顶 → 展开（Web 监听 scrollTop 差值 ±8；Android `NestedScrollConnection.onPreScroll`；iOS PreferenceKey 读内容 minY）。收起后刚好放 5 行贴纸。② 贴纸 / 表情页顶部条**横向拖动时展开成两行**（封面下带名字，1.5 秒不动收回；Android 用 `LazyListState.interactionSource.collectIsDraggedAsState`，iOS `simultaneousGesture(DragGesture)`，程序滚动不触发）。③ 顶部条：贴纸页 = ⊕商店 / 🕒最近 / 我的包封面… / **库里没加的包封面带红色 +（点即添加）**；表情页 = 🕒 / 8 个分类图标。内容是所有分区连续滚动，滚到哪个分区封面跟着亮，点封面跳分区。④ 顶部条下面一行**搜索 + 8 个快捷 emoji**（❤️👍👎🎉👋😀😢😠）：贴纸页按贴纸 emoji / 包名过滤（**只搜自己库里的，不搜 Telegram**，结果里未添加的包带「添加」）；表情页按中英文关键词搜；GIF 页当搜索词。⑤ 表情模式左下 🌐 = 收面板回键盘（三端：Web focus input / Android `FocusRequester`+`keyboard.show()` / iOS `@FocusState=true`），右下 ⌫ = 删输入框末尾一个字素（`dropLastGrapheme`：Web `Intl.Segmenter`、Android `BreakIterator`、iOS `dropLast()`）；贴纸模式右下 ⚙ = 管理我的贴纸。⑥ GIF 页**没有封面条只有搜索行**，3 列瓦片，滚到底翻页，空搜索 = 热门；顶部先放「最近使用」。⑦ 面板高度：Web `min(400, max(320, 0.9×宽))`；Android `0.92×屏宽 clamp 320~420dp`；iOS 记住上次键盘高度（`KeyboardHeight`，UserDefaults `kb_height`）去掉底部安全区，默认 336。⑧ 上次用的模式记本地（`pw_emoji_panel_mode` / prefs `panel_mode` / `emoji_panel_mode`），默认贴纸。
    - **搜索行按 TG 截图重做（2026-09-21 下午，操作者反馈）**：不再是「小搜索框 + 一排彩色 emoji」，而是**一整条圆角胶囊**：左 🔍「搜索」，右边一排**虚化**（灰度 + 45% 透明）的快捷 emoji，被点亮的那个才恢复彩色。**贴纸页的搜索行整条是按钮，效果和「+」一样**——弹表情商店 sheet 并聚焦搜索框；点快捷 emoji 直接带着它进 sheet 搜（sheet 搜索现在按贴纸 emoji 匹配，命中时每行预览只放命中的贴纸）。贴纸页原来的就地搜索结果分区已删。表情 / GIF 页仍是就地搜：点胶囊变输入框、快捷 emoji 当过滤词。三端：Web `emoji-panel/shared.tsx SearchRow(onTap?)` + `StickerStoreSheet(initialQuery, autoFocus)`；Android `SearchRow(onTap)`（灰度 emoji 用 `GrayEmoji`：画到位图套饱和度 0 的 ColorFilter）+ `StickerStoreSheet(initialQuery, focusSearch)`；iOS `SearchRow(onTap:)`（`.grayscale(1)`）+ `StickerStoreSheet(initialQuery:focusSearch:)`。
    - **GIF / 表情的搜索也改成弹框（2026-09-21 下午，操作者要求三个模式一致）**：GIF 页、表情页的搜索行同样整条是按钮，点了弹**搜索 sheet**（顶部搜索框自动聚焦 + 一行快捷 emoji + 结果区）：GIF 搜索 sheet 输入防抖 400ms、3 列瓦片、滚到底翻页、点了 GIF 发送并关闭；表情搜索 sheet 按中英文关键词搜，点了插入输入框但 sheet 不关（可连点几个），「完成」收起；空词时分别显示热门 / 最近使用。面板本体的 GIF 页只剩「最近使用 + 热门」，表情页只剩分类网格。三端：Web `emoji-panel/SearchSheets.tsx`（`GifSearchSheet` / `EmojiSearchSheet`）；Android `EmojiPanel.kt` 的 `GifSearchSheet` / `EmojiSearchSheet`（共用 `SearchSheetShell`、`GifFeed`/`rememberGifFeed`/`GifPaging`）；iOS `EmojiPanelViews.swift` 同名（`GifFeed` ObservableObject、`GifGrid`、`SearchSheetShell`）。`EmojiPanel` 的 sheet 状态统一成 `{kind: store|manage|gif|emoji, query?}`。
    - **iOS 两个修复**：① GIF 瓦片错位——`AnimatedImageView`（UIViewRepresentable）自己报的尺寸不一致导致 3 列行错位，改成 `Color.clear.aspectRatio(1)` 先定正方形再 `overlay` 图（`GifGrid`）。② 表情页往下滑卡死——`LazyVGrid` 嵌在 `LazyVStack` 里失去懒加载，1800 多个 emoji 一次全建；改成每 8 个一行（`EmojiRows`），行作为 LazyVStack 元素真正懒加载，末行用空格子补齐等宽。
    - **iOS 贴纸 / GIF 页滑动卡 + 顶部条胶囊收不起来（2026-09-21 晚）**：① 卡——贴纸页每包一个 `LazyVGrid`、GIF 页 `GifGrid` 也是 `LazyVGrid`，都嵌在 `LazyVStack` 里 → 失去懒加载，一包上百张动图 / 几十个 GIF 一次全建全播；和表情页同一招改成每行一个元素（`StickerRows` 5 列、`GifGrid` 改 3 列行式，翻页在倒数第 2 行 onAppear 触发）。② 收不起来——`PanelChrome.onOffset` 用**单帧差值**判 8pt 阈值，iOS 每帧回调一次，ProMotion 120Hz 正常滑动每帧只有 2~3pt，永远到不了；改成**同方向累计**（换方向清零，±12pt 触发）。Android（`PanelChrome.connection` 累计 ±30px）和 Web（`useScrollChrome` 累计 ±12px）同步改成累计，避免高刷屏同样问题。**Android / iOS 需重编**，Web 已部署。
    - **iOS 再修（同晚）**：① 改行式后不同包的贴纸互相串行——`LazyVStack` 把嵌套 `ForEach` 拍平成一个列表，各包的行都用 0/1/2… 当 id 撞车；现在行 id = `"分区key-r行号"`（`chunkRows()` / `GridRow`，贴纸 / emoji / GIF 三处都用）。② 累计判定后顶部条还是不收——根因是 **GeometryReader + `.coordinateSpace(name:)` + preference 那套在真机上根本不回调**；换成 `ScrollOffsetObserver`（UIViewRepresentable 塞进 ScrollView 内容，挂窗口后沿 superview 找到宿主 UIScrollView，KVO `contentOffset`），分区高亮改用全局坐标（标题 `.global` minY 减 ScrollView 自身 `.global` minY，`ScrollTopKey`），不再依赖命名坐标系。**iOS 需重编。**
    - **iOS 三修（同晚）**：① 收起 / 展开忽隐忽现——切换后顶部条高度变了 ScrollView 重排、offset 抖一下又被当成反向滚动；顶 / 底回弹也会反向。`PanelChrome.onOffset(top:maxTop:)` 改成 Telegram 手感：往下累计 24pt 收、往上累计 40pt 才展开（收比展容易）、顶部 12pt 内必展开、底部回弹（top > maxTop）和切换后 300ms 内忽略；`ScrollOffsetObserver` 回调多给 maxTop。② 还卡——一屏几十个动图全按源帧率（30fps）解码把 CPU 吃满。`AnimatedPlayer` 加 `maxFps` 跳帧（跳过的帧时长累加进等待，总时长不变）：面板 / 待发小图（≤80pt）和 GIF 瓦片限 12fps，气泡 30fps；~~Lottie 加 `respectAnimationFrameRate` + `shouldRasterizeWhenIdle`~~——**已回退**：这两项只有 Main Thread 渲染引擎支持，默认的 Core Animation 引擎会走 `LottieLogger.assertionFailure`，Debug 包直接卡死在断言上。Lottie 保持默认配置。**iOS 需重编。**
    - **收起 / 展开判定三端统一成「锚点」算法（同晚，Android 也忽隐忽现）**：逐帧方向 + 换向清零对手指抖动太敏感。现在：pos = 累计滚过距离（往下为正）；展开态锚点跟着最低点走，pos 比锚点大 24dp 就收；收起态锚点跟着最高点走，锚点比 pos 大 40dp 才展开；切换后 300ms 忽略；到顶必展开（iOS/Web 判 top<12，Android 靠 `ShowAtTop`）。Android `PanelChrome(density)`（`onPreScroll` 的 available.y 是 px，阈值按 dp 换算）；iOS `PanelChrome.onOffset(top:maxTop:)`；Web `useScrollChrome`。**Android / iOS 需重编**，Web 已部署。
    - **iOS 弹框来回滑卡顿（GIF 搜索 / 表情商店 sheet）**：两个原因。① `CGAnimateImageDataWithBlock` 在**主线程**逐帧解码，GIF sheet 一屏 20 多个动态 WebP 瓦片同时播就把主线程占满；② 商店 sheet 每行 6 张预览走 `RemoteImage` / 动图首帧，都是主线程按原图（512px）解码，LazyVStack 滑走即销毁、滑回再解一遍。改法（`StickerViews.swift`）：`AnimatedImageView` 换成自写 `AnimatedPlayer`——共用一个后台并发队列逐帧解码（`CGImageSourceCreateThumbnailAtIndex` 按显示尺寸×scale 降采样），只把解好的一帧丢回主线程，任一时刻只持有当前帧；文件数据 `AnimatedImageCache`（NSCache 64MB，同 url 并发只下一次）；新增 `StaticThumbCache` / `StaticThumbView`：后台降采样解一张位图后缓存 UIImage，`StickerImageView(autoplay:false)` 的所有格式（静态 WebP / 动图首帧 / Lottie thumb）和静态贴纸都走它。**iOS 需重编。**
    - **iOS 贴纸 / GIF 页滑动卡顿第四轮（2026-09-21 深夜，按「流畅优先：滚动中静帧、停下再播」策略）**：没有 Xcode 抓不了 Instruments，按代码能确定的三个固定开销一起改掉：
      ① **每帧 preference 归约**——分区高亮原来靠每个标题 `GeometryReader(.global)` + `SectionsKey` preference，滚动时每帧改值、整棵树重算 preference 再回调 `onPreferenceChange`（里面还算一遍 `sections`）。改成 UIKit 直读：`ScrollTracker`（一个 pane 一个 `@StateObject`）在 `ScrollTrackerView`（原 `ScrollOffsetObserver`）的 KVO 回调里，用挂在标题 `.background` 的 `SectionAnchor`（隐形 UIView）`convert(to: scrollView)` 读内容坐标，被 LazyVStack 销毁的标题位置也记着（`positions`），所以滚到大包中间也算得对；点封面 `tracker.select()` 0.6 秒内不被途经分区抢；分区列表变了 `prune()`。`ScrollTopKey` / `SectionsKey` / `scrollTop` 全删。
      ② **Lottie**——`LottieView` 原来没加 `.resizable()`，Lottie 按动画固有尺寸（TG 贴纸 512pt）布局再被 `.frame(62).clipped()` 裁，每格都是一棵 512pt、几百层带蒙版的图层树在滚动中被渲染服务合成（主线程不忙也掉帧的典型）。新 `LottieStickerView`：加 `.resizable()`；**滚动中不新建 LottieView**（图层树在主线程搭，一行 5 个进视口就是一次卡顿），已建好的 `.paused(at: .currentFrame)` + `opacity(0)`（渲染服务不合成），露出静态 thumb；停下后再建，小图随机错开 0~0.4 秒（几十个同时建会顿）。没 thumb 的 Lottie 只能停在当前帧继续显示。
      ③ **滚动中静帧**——`ScrollTracker.scrolling`（150ms 没有 offset 变化算静止）通过环境值 `stickerPlaybackPaused` 下发到网格（贴纸页 / GIF 页 / GIF 搜索 sheet 的 LazyVStack 上 `.environment`），`StickerImageView` / `GifGrid` 把它传给 `AnimatedImageView(animate:)`。`AnimatedImageView.updateUIView` 同 url 只切播 / 停不重建；`AnimatedPlayer` 改成可暂停 / 恢复（`setPlaying`，代数 `gen` 让旧 tick 链失效），且 **init 不再碰 ImageIO**（原来主线程建播放器时逐帧 `CopyPropertiesAtIndex`，一行 5 个 100 帧的 WebP 就是几百次），容器解析 / 帧时长都在解码队列上懒做。顺带：顶部条封面 `RemoteImage` → `StaticThumbView`（收起 / 展开整条重建时不再主线程解 512px 原图）。气泡 / 评论 / 待发小图没设环境值，行为不变。**iOS 需重编**；若还卡，下一步是 Instruments Time Profiler 看主线程 vs 渲染服务（Core Animation 模板看 layer 数），或服务端给面板出 128px 小尺寸动图。
    - **Android 树洞详情评论区重排（2026-09-21）**：原来是聊天气泡式（头像底对齐 + 灰气泡 + 右下「回复 时间」），操作者嫌难看。改成和动态详情一致的平铺列表 `TreeholeCommentRow`：头像 32 顶对齐 | 昵称（配色）· 时间 …… 右侧「回复」/ 正文（`@被回复人` 玫红高亮）/ 贴纸 / 细分割线；标题「全部评论（N）」。输入框占位改短「说点什么…」并 `maxLines=1`（原长提示折成两行把输入框撑高）。**iOS（`TreeholeViews.swift` 详情 ForEach）/ Web（`Treehole.tsx` + `theme.css` `.th-comment` 重写、`.th-divider` 删）已同步成同一样式。Android / iOS 需重编，Web 已部署。**
    - **表情搜索 sheet 打开不自动弹键盘**（三端 `EmojiSearchSheet`：`SearchSheetShell` 加 `autoFocus` 参数，表情传 false；GIF / 贴纸商店仍自动聚焦）——先看最近使用 / 快捷 emoji，要搜再点输入框。
    - **Android 语音图标对齐 iOS（2026-09-21）**：`ui/ChatIcons.kt` 新增 `WaveformIcon`（SF `waveform`：7 根圆头竖条）和 `KeyboardIcon`（SF `keyboard`）；聊天输入栏左侧按钮 = 文字模式 waveform / 语音模式 keyboard（与 iOS 一致）、录音中提示和语音气泡也用 waveform。原 `VoiceIcon`（喇叭）只留给通话页「免提」，并重画成 SF `speaker.wave.2` 的样子。**Android 需重编。**
    - **Android 点击穿透到主页底栏「+」**：子页面（`MainActivity.page()` 的容器 Box）只有 `background`，不参与命中测试，表情面板底部胶囊两侧等空白处的点击穿到常驻底层的 MainScreen 底栏发布按钮上。修复：容器 Box 和 `EmojiPanel` 根 Box 都挂一个什么都不做的 `pointerInput`，让整页算"命中"、到此为止（和主壳 `Pane` 同一招）。
    - **表情商店改成底部 sheet**（Web `StickerStoreSheet` 固定 86% 高、Android `ModalBottomSheet` 90%、iOS `.sheet`）：顶部搜索 + 「完成」，每行 包名 / N 张贴图·静态或动态 / 前 6 张预览 / 右侧 添加 或 已添加（点已添加 = 移除）。⚙ 进来的是同一个 sheet 的 manage 模式（置顶 / 移除）。**不做 Premium 自定义 emoji 包。**
    - **Emoji 数据**：后端 `src/emoji/emoji-data.json`（144KB，1849 个，Emoji ≤14 保证 iOS 15.4 / Android 13 都能显示），Telegram 式 8 组（笑脸与人物 / 动物与自然 / 食物与饮品 / 旅行与地点 / 活动 / 物品 / 符号 / 旗帜），每项 `[emoji, 中文名, 中英文关键词]`；由 `emojibase-data@17` 的 zh+en 生成（生成脚本很短，见本条末尾），`GET /emojis?ver=` 无需登录、按 version 增量，三端本地缓存（`pw_emojis_v1` / prefs `peiwan_emojis` / UserDefaults `emoji_groups`）。**没用 Telegram 的 GetEmojiKeywords**（emojibase 的中文标签已够用，少一个 Telegram 依赖）。
    - **GIF（后端 `src/gif/`）**：走已登录 Telegram 账号问 **@gif inline bot**（`messages.GetInlineBotResults`，bot 用 `getInputEntity('gif')` 转 `InputUser`，peer=`InputPeerSelf`），线上实测正常（返回 `BotInlineMediaResult` + mp4 document，每页 50 条，`nextOffset` 翻页；也兼容 `BotInlineResult`+WebDocument 走 fetch）。**每条下载转存 MinIO**（国内客户端拿不到 TG/Tenor 文件）：mp4 原样（GIF 图用 ffmpeg 转 mp4），再用 ffmpeg 出 **160px/12fps/3 秒 动态 WebP 预览**（`gif-thumb/`，~5KB）当网格瓦片；≤5MB。表 `gif`（key=TG document id 唯一、url/thumb/w/h/duration/size/query/hits/blocked/lastUsedAt）。**首次搜一个词要下几十个文件，给 12 秒预算**：来得及的先回，其余后台继续，结果页只短缓存让下一次拿到补齐的（客户端结果 <10 条且有 next 时 3 秒后自动再拉一次）。结果页缓存热门 10 分钟 / 搜索 30 分钟；启动 60 秒后预热热门页并每 10 分钟刷；**LRU 上限 3000 条**，每小时按 lastUsedAt 清最久没用的（连文件）。接口 `GET /gifs?q=&offset=` → `{items:[StickerPayload format=mp4], next}`。后台「Telegram 来源 → GIF」（`views/tg/TgGifCache.vue`）：统计 / 列表（按搜索词过滤）/ 屏蔽（删文件、记录保留不再入库）/ 解除 / 清空。
    - **GIF 复用贴纸通道**：`StickerPayload.format` 多了 `mp4`（`thumb` 是动态 WebP 预览）；聊天消息仍是 `type=sticker`（**照贴纸扣费**），评论 `stickerId` 传 `g<id>`（`StickerService.payloadOf` 认 `g` 前缀查 gif 表）。渲染：Web `<video muted loop playsInline autoplay poster=thumb>` 进视口才播；Android `ExoPlayer` + `PlayerView`（无控制条、静音、`REPEAT_MODE_ONE`，首帧前垫预览图）；iOS `GifVideoView`（`AVPlayerLayer` 静音、播完 seek 0，`readyToPlay` 后隐藏预览）。气泡里 GIF 220dp 圆角（贴纸 140）、评论 160；会话列表 / 通知预览「[GIF]」。「最近使用」贴纸和 GIF 分开存（`pw_gif_recent` / prefs `recent_gif` / `stk_recent_gif`，各 24 个）。
    - 后端、Web、管理后台已部署并验证（`/api/gifs` 热门 50 条 0.8s，`cat` 首次 12.9s 回 35 条；MinIO 已存 143 条 34MB）。**Android/iOS 需重编**（本机无 Android SDK / Xcode，Kotlin 与 Swift 只做了人工检查，iOS 新文件 `EmojiPanelViews.swift` 由 Xcode 同步组自动纳入）。
    - emoji 数据重新生成（换版本 / 改分组时）：临时目录 `npm i emojibase-data@17`，脚本读 `emojibase-data/zh/data.json` + `en/data.json`，按 `group`（0+1 合并、跳过 2 component）、`version<=14`、`order` 排序，输出 `{version, groups:[{key,name,icon,items:[[emoji,label,keywords]]}]}` 到 `houduan/src/emoji/emoji-data.json`，并把 `version` +1。
- **Android / iOS 整体切换浅色主题（2026-09-19）**：纯黑底白字长时间看费眼，改成 Telegram iOS 浅色那套搭配：白底 `#FFFFFF`、卡片/顶底栏 `#F5F5F8`、输入框/未选中胶囊 `#EBEBF0`、分割线 `#E5E5EA`、正文 `#111114`、辅助字 `#8E8E93`、更淡 `#B5B5BC`；强调色仍是玫红渐变。只改统一配置：Android `ui/theme/Color.kt` + `Theme.kt`（darkColorScheme→lightColorScheme）+ `MainActivity` 状态栏改深色图标（`SystemBarStyle.light`）；iOS `Theme.swift` + `PeiwanIosApp` `.preferredColorScheme(.light)`。顺带修了几处写死"白字/白色半透明底"在白底上看不见的地方：自己的聊天气泡改浅玫红底 `#FFE1E7` + 深色字（`BubbleMine` / `Theme.bubbleMine`，ChatScreens/AiChat/ChatViews 的 `mine ? white : text` 去掉）、底栏由纯黑改 `Bg2`、发布加号改玫红渐变、树洞/资料页的 `white.opacity(0.05~0.08)` 底改 `black.opacity(0.04~0.05)`。通话页（`CallBg`）、抖音模式、大厅 WebView 仍是黑底，不受影响。Web/H5（含 App 内嵌大厅）随后也切了同一套：`web/src/theme.css` 的 `:root` 变量 + 底栏/气泡/几处写死 `#2a2a30`、`rgba(255,255,255,0.0x)` 改成变量或 `rgba(0,0,0,0.05)`。**Android/iOS 需重编。**
- **大厅新增「养眼图片」tab（2026-09-19，名称后台可改，按性别分流）**：男用户看来源 A（`@mtyQCMN`），女用户看来源 B（`@wanghong_channel`），内容是 Telegram 频道的图片 / 视频（相册合成一条），走已登录的 Telegram 账号读取（网页预览拿不到视频、也抓不到关闭预览的频道）。
  - 后端 `src/gallery/`：表 `gallery_source`（channel+audience 唯一，audience 1=男看 2=女看）、`gallery_post`（sourceKey=tg:频道:m<msgId>|g<groupedId>，media JSON `[{type:image|video,url,cover,w,h,duration,size}]`）；设置在 `sys_setting.gallery_title / gallery_days`（默认「养眼图片」/ 3 天，后台可改）。每 10 分钟同步：`getMessages` → 按 groupedId 合并相册 → 图片下载原图、视频 ≤80MB 下载 + 取内嵌封面 → `putInternal('gallery'|'gallery-video'|'gallery-cover')`；每轮每来源最多 60 个文件 / 500MB，游标只推进到已处理的组；文案 `cleanText()` 去链接/@行和 VPN/防走丢/广告联系/👉 引流行；保留期 N 天 + 每受众 500 条上限，过期连文件删。接口：`GET /gallery?beforeId=`（按性别）、`GET /gallery/settings`；后台 `/admin/gallery/settings|sources|sources/preview|sources/:id/sync|posts|posts/:id/delete|purge`。
  - **注意两个频道更新都不频繁**（男频道最近一条 8 月 3 日，女频道 9 月 9 日）：按 3 天保留会空白，上线时把保留天数设成了 **60**，后台「Telegram 来源 → 养眼图片 → 设置」可改。
  - Web `pages/Gallery.tsx`：Telegram 式相册拼贴（1 张按原比例通栏；2 两列；3 上一下二；4 田字；≥5 首行两张其余每行三张），视频格显示封面 + 时长 + 播放键；图片懒加载、滚到底自动翻页、下拉刷新；点图/点视频 → **App 内走原生查看器**（`bridge.viewNativeMedia`），浏览器走网页灯箱（`<video controls>`）。`Hall.tsx` 新增 tab，标签文字取 `/gallery/settings`。
  - **H5 → 原生媒体查看器桥**：`viewNativeMedia(items[{type,url,cover}], index)`；iOS `postMessage({type:'viewMedia',items,index})`，Android `PeiwanNative.viewMedia(itemsJson, index)`。原生：Android `ui/Common.kt` `MediaViewer`（HorizontalPager + Telephoto 缩放图 / ExoPlayer 视频页，`HallScreen` 用全屏 Dialog 展示）；iOS `MediaViewerView.swift`（TabView + ZoomableImage / AVKit VideoPlayer，`HallView` fullScreenCover）。老版本 App 识别：Android 直接探测方法；iOS 由原生注入 `window.__peiwanBridgeV2=true` 标记，没有就走网页灯箱。
  - **后续调整（2026-09-19 晚）**：tab 名称与保留天数都按男/女分开（`sys_setting.gallery_title_m/f`、`gallery_days_m/f`，同步/列表/清理各按自己的天数）；用户端**最新在最底部**（像聊天记录，打开滚到底、上滑加载更早、视口不跳）；查看器改为**上下滑切帖子、左右滑切图**（桥载荷 `groups/group/index`，Android `viewMediaGroups`，iOS 同 `viewMedia` 消息带 `groups`）；来源可填「屏蔽词」，内置广告行特征扩充，文案像广告只丢文案不丢图；卡片不显示频道名。男用户那个涉黄来源（`@mtyQCMN`）已删除，等操作者找新频道。
  - **分享（2026-09-21）**：每张卡片左下「分享」→ Web `bridge.shareText()`：Android 走新桥 `PeiwanNative.shareText(text,title)`（`HallJsBridge`，系统分享面板；老版本 App 没这个方法就回退复制链接）→ iOS WKWebView 直接 `navigator.share` → 浏览器 `navigator.share` / 剪贴板。分享地址 **`https://app.yyheart.com/s/gallery/:id`**（后端 `GET /api/share/gallery/:id` 出 og 标签：标题 = tab 名，描述 = 文案或「N 张图片 · M 个视频」，图 = 首图 / 首个视频封面），跳到公开落地页 `/#/gallery/share/:id`（`GallerySharePage`：同一套拼贴 + 网页灯箱 + 「打开 App」按钮）；数据接口 `GET /app/gallery/:id`（公开、限频，媒体地址已拼成完整 URL，文案按该受众的屏蔽设置处理）。**Android 需重编**才有原生分享面板，iOS 不用。
  - **私密树洞分享（2026-09-21）**：和养眼图片同一套。帖子卡片左下「分享」（列表卡 + 详情卡，三端）：Web `Treehole.tsx shareTreehole()` 走 `bridge.shareText()`；Android `shareTreehole(ctx, post)` 系统分享 intent；iOS `shareTreehole(post)` → `ShareSheet`。文案 = 正文前 60 字（没正文就「私密树洞 · N 张图片」）+ 短链 **`https://app.yyheart.com/s/treehole/:id`**（后端 `GET /api/share/treehole/:id` 出 og：标题 = 正文前 40 字、描述带评论数、图 = 首图）→ 免登录落地页 `/#/treehole/share/:id`（`TreeholeSharePage`：帖子卡 + 最近 10 条评论 + 「打开 App 参与讨论」）；数据接口 `GET /app/treehole/:id`（公开、限频，匿名不带 mine，图片 / 头像拼成完整 URL，`comments` 取最新 10 条）。**纯文字帖分享不带图**：og 页本来就不出 og:image；iOS / Web 纯文字帖只传一段文字（链接拼进文字，不单独传 URL 项，系统分享面板就不会去抓链接预览顶上放图标位），带图的帖才传 URL 让卡片有图；`bridge.shareText(text, '')` 支持 url 传空。后端 / Web 已部署并验证；**Android / iOS 需重编**。
    - **iOS 分享面板顶上空白图位（2026-09-21 晚再修）**：上一条「纯文字只传一段文字」没起效，面板顶部还是留着一个显示不出来的图位（系统会为文字里的链接 / URL 项去抓 og 图，纯文字帖没有图就一直空着）。改成自己给预览：`Compat.swift` 新增 `ShareLinkItem: UIActivityItemSource`，`activityViewControllerLinkMetadata` 返回 `LPLinkMetadata`（标题 = 文案，`imageProvider` = 帖子首图 / 没图用 `iconProvider` = App 图标 `ShareSheet.appIcon`），系统就不再去抓链接。`shareTreehole`：纯文字帖传一个 `ShareLinkItem(payload: 文案+链接)`；带图帖先取首图（`RemoteImageCache` 没有就下载，超时 3 秒）再弹 `[ShareLinkItem(image:), link]`。音乐分享 `MusicCenter.shareLink` 仍是 `[text, url]`（有封面 og:image 能抓到，先不动；要统一也套 `ShareLinkItem` 即可）。**iOS 需重编。**
  - **屏蔽文字（2026-09-21）**：男/女频道各一个开关「屏蔽文字」（`sys_setting.gallery_hide_text_m/f`，`'1'|'0'`，**默认男频屏蔽、女频不屏蔽**）。在 `GalleryService.list()` 出口把 `text` 置空（不改库，切换即时生效可反悔），`GET /gallery/settings` 多回 `hideText`；后台「Telegram 来源 → 养眼图片 → 设置」两行各带一个复选框。**后台「已同步内容」也跟着屏蔽**（操作者要求）：`adminPosts` 对开了屏蔽的受众把 `text` 置空并回 `textHidden:true`，后台那格只显示「已屏蔽文字」标；库里原文不动，关掉开关就回来（保存设置后列表自动刷新）。已部署。
  - **iOS 查看器 × 点不到（2026-09-21 修）**：`MediaViewerView` 整页 `ignoresSafeArea()` 后顶部条 `padding(.top, 8)` 压在状态栏 / 灵动岛下面。改为 `padding(.top, topInset() + 8)`（从 keyWindow 取 `safeAreaInsets.top`，取不到按 47），按钮加大到 44pt 并 `zIndex(10)`。**iOS 需重编。**
- **大厅 tab 顺序后台可调（2026-09-21）**：`sys_setting.hall_tabs` 存 JSON 数组（key 固定四个 `guide/games/gallery/treehole`，漏掉的按默认顺序补到末尾，`HallTabsService.normalize`）。用户端 `GET /modules/hall-tabs` → `{order}`；后台 `GET/PUT /admin/hall-tabs`（`views/ModulesView.vue` 顶部新卡「大厅 tab 顺序」，◀▶ 挪位）。Web `Hall.tsx` 按 order 排 tab，**默认打开排第一的 tab**（本会话手动切过则记住），顺序缓存 sessionStorage `hall_tab_order`；App 内嵌仍不显示树洞。只有 H5 改动，App 内大厅是 WebView 不用重编。已部署。
  - **频道转发（推广小功能，2026-09-22）——注意：`@smsd2030` 是操作者自己的推广频道，不参与 App 任何内容，别把它加成树洞 / 养眼图片来源**（之前有一轮误把它种进了 `treehole_source`，代码已回退、线上来源 / 帖子 / `treehole_seeded_sources` 标记已删）。功能是反过来的：后台配置若干**别人的公开频道**当来源，服务端每 10 分钟用已登录的 Telegram 账号把它们的新消息 `messages.ForwardMessages` **转发到 @smsd2030**。
  - 后端 `src/tgforward/`（`TgForwardService`，只挂在 AdminController 上、无用户端接口）：表 `tg_forward_source`（channel 唯一 / title / subscribers / enabled / `dropAuthor` 隐藏「转发自」头默认开 / `mediaOnly` / `maxPerRun` 每轮上限默认 3 / `blockWords` / `lastMsgId` / `forwardedCount` / lastError）；目标频道存 `sys_setting.tg_forward_target`（默认 `smsd2030`，保存时校验账号是该频道管理员且有发布权限：`channel.creator` / `adminRights.postMessages` / `channels.GetParticipant(InputPeerSelf)`）。添加来源时游标定在当前最新一条，**只转之后新发的**；相册（groupedId）整组一起转仍是一条相册；每条间隔 2.5 秒；来源开了「禁止转发」（`noforwards`）拒绝添加、转发报 `CHAT_FORWARDS_RESTRICTED` 记到 lastError。日常按时间顺序走：不合格（屏蔽词 / 只要媒体 / 空）跳过推进游标，合格的转完再推进，满 maxPerRun 停下留给下一轮，中途报错游标停在最后一条成功的。
  - 接口：`GET /admin/tg-forward/status`（目标 / 标题 / 订阅 / canPost / 错误）、`PUT /admin/tg-forward/target {channel}`、`GET /admin/tg-forward/sources`、`GET /admin/tg-forward/sources/preview?channel=`（频道信息 + 最近 8 组消息 + noforwards）、`POST /admin/tg-forward/sources`（body 含 `backfill` 新建时立刻把最近 N 条转过去，≤20）、`DELETE /admin/tg-forward/sources/:id`、`POST /admin/tg-forward/sources/:id/sync?backfill=N`（不带 = 立即检查新消息；带 = 不看游标补最近 N 条）。
  - 后台「Telegram 来源 → 频道转发（推广）」（`views/tg/TgForward.vue`）：上卡目标频道 + 账号权限状态；下卡来源（解析 → 添加，可设每轮上限 / 屏蔽词 / 隐藏来源 / 只带媒体 / 启用 / 添加后先补 N 条），列表有「立即检查」「补最近 N 条」「编辑」「删除」。
  - 已部署并验证：`status` 回 `target=smsd2030「私密树洞」canPost=true`（登录的账号是管理员）。**操作者待办**：到后台加来源频道。
- **内容分发（推广，2026-09-22）——树洞同步进来的每条新帖自动发到小红书 / 抖音 / 快手 / 知乎（操作者要求：不挑不审，同步一条就发）**：
  - 后端 `src/publish/`：表 `publish_job`（postId+platform 唯一；status 0 待发 1 已领 2 成功 3 失败 4 跳过；scheduledAt 排期）。`PublishService` 每分钟扫 `treehole_post`（source=2、水位 `publish_last_post_id` 之后、正文 ≥30 字）→ 每个启用平台各调 DeepSeek 出一稿（按平台模板：小红书标题 ≤20 字 + 120~300 字 + 5 tag；抖音 / 快手 ≤30 字标题；知乎「想法」无标题 150~400 字；system prompt 里去隐私 / 违禁词软化 / 不带链接 App 名）→ 排期 `nextSlot()`：今天起最多往后 3 天，每天每平台 ≤ dailyMax、落在 hourStart~hourEnd（北京时间）、同平台间隔 ≥ gapMin，排不进标 4 跳过。设置在 `sys_setting.publish_*`（enabled / platforms / daily_max 默认 5 / hours 默认 9-23 / gap_min 默认 45 / token / last_post_id）；**开启时水位定在当前最新一条，旧帖不发**。领走 30 分钟没回结果自动放回；失败最多重试 3 次（每次 +30 分钟）。
  - 发布机接口（`PublishAgentController`，请求头 `X-Publish-Token`，错了回 code 401）：`POST /publish/agent/heartbeat {host, accounts:{平台:{ok,msg}}}`、`GET /publish/agent/next?platforms=`（乐观锁领一条到点的）、`POST /publish/agent/result {id, ok, url?, error?}`。后台接口 `GET /admin/publish/overview`、`PUT /admin/publish/settings`、`POST /admin/publish/token/rotate`、`GET /admin/publish/jobs?status=&platform=`、`POST /admin/publish/jobs/:id/retry|skip`、`DELETE /admin/publish/jobs/:id`、**`POST /admin/publish/test {platforms}`（拿最近一条树洞帖立刻排任务，不受上限 / 时段限制，反复试会先删旧任务）**。
  - 后台「内容分发（推广）」页（`views/PublishView.vue`，15 秒自动刷新）：规则卡（开关 / 平台 / 每日上限 / 时段 / 间隔）、发布机卡（在线 / 心跳 / 各平台登录状态 / token 显示复制更换）、测试发布、任务表（展开看原文，立刻发 / 重发 / 跳过 / 删除）。
  - **发布机 `publisher/`（Python，跑在操作者本机 DESKTOP-BNO4DSC，已安装并注册计划任务 `PeiwanPublisher` 登录自启、pythonw 后台）**：`publisher.py`（`run` 轮询 60s：心跳 → 领任务 → 发布 → 回写；`login <平台>` 弹浏览器扫码；`check`；`card` 看卡片样式）、`card.py`（patchright Chromium 把文案截成 1080×1440 卡片，>230 字自动拆多张，抖音 / 快手 / 小红书没有纯文字帖所以必须有图）、`zhihu.py`（自写「想法」适配器，持久化 profile；**选择器是按知乎当前页面猜的，第一次真发要盯一下，失败会在 logs/ 留截图**）。抖音 / 快手 / 小红书走 `vendor/social-auto-upload`（gitignore，`setup.ps1` 克隆；`sau <平台> upload-note --images ... --title --note --tags`，退出码 0 成功；上游 baijiahao uploader 还 import 的是 `playwright`，所以 venv 里 patchright 和 playwright 都装了）。`setup.ps1` / `install-task.ps1` 存成 **UTF-8 BOM**（PowerShell 5 读无 BOM 的中文脚本会解析错）。patchright Chromium 装的时候 npmmirror 没有 v1208，脚本会回退直连。
  - **小红书「账号异常提醒 / 疑似脚本自动浏览」（2026-09-23 新号第二天）**：原因是发布机每 30 分钟用**无头浏览器**跑一次 `sau xiaohongshu check`（打开创作者后台核对 cookie）+ 无头发布，规律访问 + 无头指纹被判「不常见的浏览行为」。改：① `check_min` 默认 30 → 360 分钟；`no_periodic_check`（默认 `["xiaohongshu"]`）里的平台定时检查**不开浏览器**，只看本地 cookie 文件在不在，真实状态由发布结果反映；没登录平台的复查改成看文件、5 分钟一次。② `headed_platforms`（默认 `["xiaohongshu"]`）发布时 `--headed` 有界面浏览器（发布机那台会弹窗）。③ 领到任务后随机等 20~120 秒再发；后端 `nextSlot` 排期加 3~25 分钟随机抖动。④ 建议小红书每日上限先设 1~2，且**用手机 App 每天正常刷一刷、点赞评论**积累真人行为；警告再来就把小红书改半自动（生成好卡片 + 文案推到 TG 私聊，手机手动发）。卡片样式换成 信笺 / 杂志 / 奶油 三套随机（`card.py STYLES`，`config.card_style` = random|letter|magazine|cream），视频方案（`video/`，typewriter / broll 两条管线）操作者看过样片后**放弃**，代码留着没接入。
  - **第五个平台：微信视频号 `shipinhao`（2026-09-23）**：后端 PLATFORMS / 出稿模板（title ≤22）加了；发布机登录 / cookie 校验复用 sau 的 `tencent`（`SAU_NAME` 映射，cookie 文件 `vendor/cookies/tencent_main.json`），**图文发布 sau 没做（`TencentNote` 全是 NotImplementedError），自写 `shipinhao.py`**：视频号助手 `/platform/post/create` → 找「图文」入口 → `input[type=file]` 传卡片图 → 描述框写 标题+正文+#话题 → 「发表」；元素是猜的，找不到会截图 + 把页面输入框 / 按钮文字 dump 进日志（`shipinhao-*.png`），**第一次真发大概率要对着日志改选择器**。默认有界面浏览器发（`headed_platforms` 含 shipinhao）。exe / bat 都加了视频号。
  - **文案模式开关（2026-09-24，操作者觉得 AI 改写和原文差距太大）**：`sys_setting.publish_mode` = `raw`（**默认，原文直发**：一个字不改、不过 BANNED 过滤；标题取第一句截到平台上限 小红书 20 / 抖音快手 30 / 视频号 22 / 知乎无，正文截到 1000（知乎 2000），tags 固定 情感 / 心事 / 故事）或 `ai`（原来的 DeepSeek 改写 + 过滤）。后台规则卡多一行单选；`draft()` 按 mode 分流到 `draftRaw` / `draftAi`；raw 模式不需要 AI_API_KEY。只影响之后入队的任务。
  - **文案模式改成按平台分别设（2026-09-24 同日）**：新 key `sys_setting.publish_modes` = `xiaohongshu:ai,douyin:raw,...`（`Settings.modes: Record<Platform,'raw'|'ai'>`）；没写到的平台沿用旧的全局 `publish_mode`，再没有就 raw。`PUT /admin/publish/settings` 收 `modes: {平台: 'raw'|'ai'}`（旧的 `mode` 字段已去掉）。`draft(text, platform, s)` 按 `s.modes[platform]` 分流；AI 模式缺 AI_API_KEY 时只让那个平台的任务记失败（`AI 改写失败：… AI_API_KEY 未配置`），不再整轮跳过扫描。后台规则卡「文案」行改成每个平台一个下拉。顺带修：页面 15 秒自动刷新原来会把没保存的表单改动冲掉，现在只在首次进入时回填表单。
  - **每日上限按平台分别设（2026-09-24）**：`sys_setting.publish_daily_maxes` = `kuaishou:1,shipinhao:1,...`（`Settings.dailyMaxes`，没写到的平台用 `publish_daily_max`），`nextSlot()` 按平台取上限；后台「各平台」行每个平台一个「每天 N 条」，原来的全局输入框去掉了。当前：快手 / 视频号 1，其余 3。
  - **视频任务已接入（2026-09-24，快手 / 视频号默认发视频）**：
    - 表 `publish_job` 加 `format`（note / video）和 `media`（VarChar(6000) JSON `{paragraphs, images, scenes}`；**MySQL 的 TEXT 不能带默认值，第一次用 @db.Text 导致 db push 失败 api 重启循环**）。设置 `sys_setting.publish_formats`（默认 kuaishou / shipinhao = video，知乎只能 note），后台「各平台」行多一个「图文 / 视频」下拉；任务表显示形式、「等待出图」、出好的剧照缩略图。
    - `publish-media.service.ts`：每分钟挑一条 `status=0 & format=video & media=''` 且 6 小时内要发的任务 → `splitChunks` 按句把文案均分成 4~7 段旁白（约 55 字一段，不改字）→ DeepSeek 出 `characters` + 每段一个画面描述（尺度：可性感，不裸露 / 性行为，亲密情节换暗示画面）→ 万相 `wan2.7-image` 组图（异步任务 `X-DashScope-Async` + 轮询 `/tasks/{id}`，1080*1920，n = 段数）→ 下载存 MinIO `publish/` → 写 media。同帖另一个视频平台直接复用。失败记 status 3「出图失败：…」，后台「重发」会重新出图。服务器 `.env` + compose 加了 `DASHSCOPE_API_KEY`。实测一组 4 张约 1.5 分钟。
    - 发布机：`GET /publish/agent/next` 多传 `formats=note,video`（本机装了 edge-tts / imageio-ffmpeg / pillow 才传；**老版本不传 = 只给图文任务**，视频任务出完图才下发）。`publish_video()`：下载剧照 → `video/slides.render()`（一段旁白一张图；原文模式标题就是第一句时不重复念）→ `sau <kuaishou|tencent> upload-video --file --title --desc [--tags]`（视频号短标题后端截到 16 字）。配音音色 `config.video_voice` / `video_voices{平台}`，默认 xiaoxiao。依赖：`安装视频依赖.bat`（setup.ps1 也加了）。
  - **外网：X（中文）+ YouTube（英文）（2026-09-24，TikTok 暂缓——要美国静态住宅 IP）**：
    - 后端 `PLATFORMS` 加 `x` / `youtube`，`OVERSEAS` / `ENGLISH` 常量；`FIXED_FORMAT` 知乎只能图文、X / YouTube 只能视频。英文平台出稿走 `draftEn()`（raw = 忠实翻译，ai = 英文改写，标题 ≤95 字符 + 5 个英文 tag）。出图：英文任务用中文原帖切段分镜，media 多存 `zh`（中文旁白），同帖复用别的平台的图时按 `zh` 逐段翻译成英文（`translateParagraphs`，段数不变保证一段一张图）——实测 YouTube 任务不再出图，只翻译。
    - 发布机状态按主机名分开记（`agents` Map，overview 回 `agents[]`；`GET /publish/agent/next` 带 `host`），后台发布机卡每台一行。
    - 发布机同一套代码：**出口在国内只领国内平台，出口在国外只领 X / YouTube**（`OVERSEAS` + `check_ip`）。本机 DESKTOP-BNO4DSC（出口德国法兰克福 Baxet 机房 IP）当外网发布机：`config.json` platforms 改成 `["x","youtube"]`，计划任务 `PeiwanPublisher` 还是 Disabled（等 X / YouTube 登录后再启用）。
    - X：自写 `publisher/x.py`（patchright 持久化 profiles/x，有界面；compose → `tweetTextarea_0` 输入 → `input[data-testid=fileInput]` 传视频 → 等 `tweetButton` 可点 → 发；免费账号视频 ≤140 秒，发布机按字数先砍段、合成后超 136 秒再去掉最后一段；帖子文字 = 标题 + 3 个话题）。敏感内容标记要在 X 设置里手动开。YouTube：sau `youtube upload-video --visibility public`（用本机 Google Chrome，`conf.py YT_PROXY=None` 走系统出口）。英文视频：`video_voice_en` 默认 en-US-AriaNeural、`slogan_en`、「AI generated」；字幕 `video/common.sentences()` 英文按句 / 单词折成 ≤24 字符，标题按单词折行。
    - 登录：`登录-X.bat` / `登录-YouTube.bat`。每日上限 X / YouTube 各 1。
    - **X 代币联动（操作者要求，2026-09-24）**：代币是 ARK launchpad 上 Arc 网络 USDC 计价的 meme 币 `https://ccfspt.com/token/0x870B91f9aF1f73F80E42826eb5f7400c9e97D37c`，每条 X 都带，三处都在发布机 config 里改（空 = 不带）：① `x_promo` 正文推广行（默认「第一个陪玩美女代币（USDC 链）👉 链接」，X 帖子文字 = 标题 / 推广行 / 3 个话题；「约单」两个字去掉了，怕配合敏感媒体被判招嫖）；② `x_reply` 发完自己回复一条（默认同 `x_promo`；`x.py` 从发帖成功 toast 拿帖子链接，拿不到就去自己主页找最新一条（跳过置顶）→ 回复框 → `tweetButtonInline`；**回复失败只记日志 + `x-noreply-*.png` 截图，不算发布失败**，第一次发要看）；③ `x_outro` 视频片尾卡（`slides.render(outro=[...])`，配音结束后多 3 秒：半透明黑底 + 金色大字「第一个陪玩美女代币」+ 白字「USDC 链上发行 / ccfspt.com / 链接见帖子和评论区」，字幕在片尾卡前结束，BGM 放到最后；只给 X，136 秒上限包含片尾）。代币符号那个页面抓不到（前端渲染），知道后可在 `x_promo` 里加 `$符号`。
    - **X 第一次实测（#56，帖 #57，2026-09-24 17:00，https://x.com/yinyuan659/status/2103046833572581860）**：视频传完 `tweetButton` 能找到但 `click()` 30 秒超时（被透明浮层挡住）→ `x._press()`：正常点 10 秒不行就在输入框按 **Ctrl+Enter**（X 发帖快捷键），还在就 JS `b.click()`；实测 Ctrl+Enter 发出去了，没重复。**根因（同日 #73 查清）**：打完字（尤其带链接出卡片后）X 在 `#layers` 里盖一个透明全屏 div，Playwright 报「`#layers` subtree intercepts pointer events」，按钮和输入框都点不到（人手点可以）。`_press()` 改成：正常点 5 秒 → 直接按 Ctrl+Enter（刚打完字焦点还在输入框，**别再去点 / focus 输入框**，那一步也会卡 30 秒）→ 按钮还可点再 JS `b.click()`；`_focus()` 输入框点不动就 JS focus。回复验证：输入框里推广文字清空才算成功。回复推广链接成功（X 把结尾链接渲染成 ARK 的链接卡片，账号开了敏感标记所以卡片也带遮罩）。同时修了**中文视频标题不折行、超出屏幕被截**（`video/common.wrap_cjk`，一行 10 字、尽量断在标点后；快手 / 视频号也受益）。后台启用平台已加上 x / youtube。
    - **X 美女图（TG 频道 → X 纯图帖，2026-09-24）**：`houduan/src/publish/x-pics.service.ts` + 表 `x_pic`（图片池，`jobId` 空 = 未用）+ 设置 `sys_setting.xpics_*`（enabled / channel 默认 `GSSDJLR888` / daily 默认 5 / min~max 默认 2~4 张，X 一条最多 4 张 / fetch_hour 默认 0 / last_fetch / last_result）。每分钟 tick：开启且今天没拉过、到了 fetch_hour → 用后台登录的 Telegram 账号（`TelegramClientService`）翻来源频道最近 24 小时的 `MessageMediaPhoto`（最多 150 张，<15KB 丢掉）→ MinIO `xpics/日期/` → `x_pic`；然后 `plan()` 按 daily 在内容分发的发布时段里平均切 N 个时间点（±10 分钟抖动），今天还没到的点各排一条 `publish_job{platform:x, format:pics, title/content 空, media:{images}}`（postId = 负的第一张图 id，保证唯一），取图时最新相册优先、同相册放一起。手动「立即发一条」打 `tags=manual`，不占每日次数。**pics 任务不算进 X 树洞视频的每日上限 / 间隔**（`nextSlot` 排除 format=pics）。发布机：`next` 多传 `pics`，`publish_pics()` 下载图 → `x.post_media()`（原 `post_video` 泛化成一个视频或多张图、文字可空），正文不带文字，发完照样回复 `x_reply`（USDC 代币链接）。后台「内容分发」页多一张卡：开关 / 频道 / 每天次数 / 每次张数 / 拉取时点、立即获取、立即发一条、池子剩余、今天排期、池子预览。接口 `GET /admin/xpics`、`PUT /admin/xpics/settings`、`POST /admin/xpics/fetch`、`POST /admin/xpics/post-now`。**已部署，首次拉取 96 张；开关默认关**——图是真人照片（有的是抖音主页截图），操作者确认后自己在后台打开。**看图过滤（同日）**：`x_pic.checked / skip_reason`，可用池 = 没用过 + checked + skip_reason 空。`review()` 用百炼 OpenAI 兼容接口 `qwen-vl-plus`（`XPICS_VL_MODEL` 可换）逐张看图，判截图 / App 界面、拼图九宫格、带文字水印联系方式二维码、非真人女性、疑似未成年、露点 → 不合格记原因；每分钟 tick 查 30 张（开关关着也查），拉取后立刻查完；接口失败留着下轮再查。实测抖音主页截图 →「包含手机界面和用户信息」、九宫格 →「九宫格拼图」，正常写真都过。后台卡片显示 可用 / 待检查 / 已过滤 + 最近被过滤的图和原因。发了 2 条手动测试（#64、#65）。
    - **YouTube 同样三处（英文）**：`yt_promo` 放简介第一行（默认「The first e-girl companion token (USDC chain) 👉 链接」）；`yt_outro` 英文片尾卡（「E-Girl Companion Token / The first one, on the USDC chain / ccfspt.com / Link in description & comments」，英文第一行 72 号）；`yt_reply` 发完自己评论并尝试置顶——自写 `publisher/yt_comment.py`（复用 sau 的 `vendor/cookies/youtube_main.json` storage_state，本机 Chrome 有界面；视频链接从 sau 成功输出里正则抓；Shorts 页先点 `#comments-button` 再 `#simplebox-placeholder` → `#contenteditable-root` → `#submit-button`；置顶 = 第一条评论 `#action-menu` → Pin/置顶 → `#confirm-button`），失败只记日志 + `yt-comment-*.png` 截图。**第一次实测（#50，2026-09-24 16:37，https://www.youtube.com/shorts/Vof_GXDTflM）**：① sau 上传前的 `cookie_auth` 用无头 Chrome，UA 带 HeadlessChrome 被 Studio 挡在「不受支持的浏览器」页，误报 cookie 失效 → 改成换掉 UA + 最多等 15 秒跳 `/channel/`（`patches/youtube_uploader_main.py`，setup.ps1 会覆盖到 vendor）；② sau 成功输出里没带视频链接 → `yt_comment` 兜底去 Studio `/videos/short`（再 `/videos/upload`）拿最新一条；③ Shorts 页评论按钮元素变了 → 统一改到 `watch?v=` 普通播放页评论（评论成功，watch 页上链接是蓝色可点）；④ 置顶菜单项是 `tp-yt-iron-dropdown tp-yt-paper-item`「置顶」，但**新频道点了只弹「积累频道历史记录后才能置顶」**，识别到就点「知道了」记日志，频道够格后自动置顶。**注意 YouTube 2023-08 起 Shorts 简介 / 评论里的链接不能点**（我们的竖版视频 ≤3 分钟都算 Shorts），能点的只有频道主页的「链接」栏——让操作者在 YouTube 频道自定义里加这个链接。
    - **外网机也用 `发布机.exe` 操作（操作者要求，2026-09-24）**：`publisher_gui.py` 的平台列表改成按 `config.json` 的 `platforms` 显示（外网机只显示 X / YouTube，标题「外网发布机」，不显示「看卡片样式」，多一行 X 敏感内容设置提示）；出口 IP 按 `publisher.py check` 的「→ 国内 / → 国外」判断，外网机国外出口才标绿；「启动 / 重启」遇到计划任务是 Disabled 会先 `Enable-ScheduledTask`（普通权限可以）。exe 不进 git（`*.exe` ignore），改 `publisher_gui.py` 后在 publisher 目录跑 `build-exe.ps1` 重打；国内机要拿新 exe 覆盖。
  - **视频样片（2026-09-24）**：`publisher/video/slides.py`（连续剧照 + 慢推拉 + 交叉淡化 + edge-tts 配音 + 逐字字幕，图片数 = 段落数时一段旁白一张图，按配音时间切）、`video/imagegen.py`（免费生图：Pollinations 无 key 带水印裁掉 / HF FLUX.1-schnell 匿名配额几乎为 0）；`python video_demo.py 3` 出样片。结论：免费模型能让单个主角基本一致，但道具 / 动作不听话、两人同框会丢人，操作者看过说不行 → 改用万相 2.7 组图（`wan2.7-image`，`enable_sequential=true`，0.2 元/张），**待操作者给百炼 API Key**。
  - **队列不越积越多（2026-09-24）**：Telegram 每天同步约 6 条新帖、每条给每个启用平台各排一条；原来 `MAX_DAYS_AHEAD=3`（排不进往后找 3 天），每日条数少的平台（快手 / 视频号 2、X / YouTube 1）队列常年排满到 3 天后、发的都是旧帖。改成后台设置 `publish_queue_days`（「当天满了最多往后排 N 天」，默认 1，0 = 只排当天），再排不进的新帖直接记「跳过」（跳过记录只存原文没出稿，别点重发）。同时 抖音 / 知乎 每日上限调到 6（能跟上每天的新帖量）。
  - **抖音图文标题上限 20 字（2026-09-24）**：sau 抖音图文 `validate_upload_args` 限 20 字，后端原来按 30 截 → 发布机报「标题不能超过20字符」。`TITLE_MAX.douyin` 改 20（AI 模板同步），`claim()` 下发时再按平台上限截一次（已入队的旧任务不用重出稿）。
  - **踩坑**：计划任务的动作不能是 `cmd /c start ... run.bat`——cmd 退出后任务算结束，且重复 Start 会留下多个实例（日志里出现两条紧挨的 WARNING 就是多实例）；现在直接 `pythonw.exe publisher.py run`，`MultipleInstances IgnoreNew`，失败 1 分钟后自动重启。
  - 已部署并验证：后端接口、心跳（后台显示在线 + 四平台未登录）、卡片渲染。**操作者待办**：本机依次 `login xiaohongshu / douyin / kuaishou / zhihu` 扫码 → 后台「内容分发」点「立刻测试一条」看结果 → 满意后把规则卡开关打开。第一次知乎发布要看 `logs/`。
  - **第一次实测（同晚）的结果与修正**：① 小红书发成功了，但**账号当场被冻**——判「非官方渠道导流 + 高风险交友」，原因是卡片底部印着「心之音 · 私密树洞」品牌 + 标语「匿名说心事…」+ 交友向 tag。改：卡片默认**不放品牌 / App 名**，只放标语（`config.brand` 空、`slogan` 默认「爱情与金钱无关，和内心相连」，操作者定的）；AI system prompt 加硬性禁词（微信 / 私信 / 加我 / 找对象 / 脱单 / 相亲 / 交友 / 同城 / App 名…），tag 只能从固定池（情感 / 心事 / 成长 / 生活感悟…）里挑；后端出稿后再过一遍 `BANNED` 正则，命中就重来。② **本机开着 VPN（TUN 全局），发布被定位到德国**。发布机加出口 IP 检查（`check_ip()`，ip-api.com，5 分钟缓存）：不在中国大陆就**不领任务**，心跳把 `ip` 报给后台，后台发布机卡显示「国外（VPN？已停发）」；浏览器加 `--no-proxy-server` 实测绕不开 TUN，所以要么发布时关 VPN / 切规则模式，要么把发布机搬到另一台没 VPN 的电脑（`publisher/README.md` 有迁移步骤）。③ 知乎 `/pin` 页已 404：想法编辑器在**首页** `.WriteArea`（占位「分享此刻的想法...」→ 点开是 `textarea[标题]` + Draft.js `.public-DraftEditor-content[role=textbox]` + 「发布」按钮），而且 **headless shell 渲染不出这个编辑器，必须 `channel="chromium"`（完整 Chromium 的 new headless）**；编辑器自动存草稿，输入前先 Ctrl+A Delete。`post_pin(dry_run=True)` 可以只走流程不点发布（已 dry run 通过）。④ exe 加「登出 / 换号」（`publisher.py logout <平台>`：删 `vendor/cookies/<平台>_main.json` 或 `profiles/zhihu`）和出口 IP 显示。⑤ 抖音 / 快手图文发布时 sau 走的是「转载」声明，对不是自己原创的树洞内容这是对的，别改成原创。**已部署；操作者待办：换小红书新号，决定发布机放哪台电脑。**
- **管理后台新增「Telegram 来源」菜单**（`views/TelegramView.vue` + `views/tg/*`）：Telegram 账号登录卡 + 三个分区（养眼图片：设置/来源/已同步内容；音乐：来源；私密树洞：来源）。原「音乐频道」页只剩曲目管理，「私密树洞」页只剩帖子管理。
- **H5 → 原生音乐播放器桥（2026-09-19，约定：以后大厅 H5 里播音乐一律走原生）**：`web/src/bridge.ts` 新增 `nativeMusic(action, track?, queue?, {ratio|value})`，action = `play|pause|resume|stop|seek|rate`；iOS 走 `webkit.messageHandlers.peiwan.postMessage({type:'music',...})`，Android 走 `PeiwanNative.music(json)`。原生用 `MusicCenter`（与原生音乐页同一个播放器：后台可播、消息页顶部栏 / 锁屏可控），切歌/暂停时回推 `window.PeiwanMusicState({id, playing})`。`web/src/music.ts` 的 `MusicStore` 在 App 内嵌时自动全部委托原生（不再用 `<audio>`），首次 play 1.5 秒没收到回推判定老版本 App 无此桥 → 退回 `<audio>`。接收端：Android `HallScreen.kt` `HallJsBridge.music()` + `LaunchedEffect` 回推；iOS `HallViews.swift` `Coordinator.handleMusic()` + Combine 订阅回推。**以后 H5 里任何播音乐的地方直接用 `music.play(track)`（music.ts）即可，不要自己建 `<audio>`。**
- **Android 事件穿透修复（2026-09-19）**：主壳 `MainScreen.Pane` 用 `drawWithContent` 跳过非当前 tab 的绘制，但 `background` 不参与命中测试，当前页空白处的点击在 hit-test 时会穿到下层兄弟（如「我的」）的按钮上。修复：给 Pane 挂 `pointerInput`——当前页只是"命中"不处理（命中测试到此为止），非当前页在 Initial pass 把事件全部 consume 兜底。
- **音乐保留规则改为按数量**：不看天数，最多 100 首（`MusicService.MAX_TRACKS`），超出按发布时间删最旧的连文件删；同步不再跳过 3 天前的消息（首次扫最近 60 条）。三端与后台文案同步改成「最多保留 100 首」。
  - **多来源（2026-09-21）**：最多 **5 个来源**（`MAX_SOURCES`，新增时超了报错，后台按钮也禁掉），**每个来源各有自己的保留上限** `music_source.max_tracks`（默认 100，后台来源表单可设 1~500，已 db push；`purgeOld` 按 sourceId 用各自上限清，`purgeSource()`；把上限调小时保存后立刻清一次不用等下一轮）。同步逻辑不变（每 10 分钟轮流同步各启用来源）。`GET /music` 返回所有启用来源的歌混排、最新在前（一次最多 500 首，客户端不翻页），多了 `sources:[{id,title,channel}]` 和每首的 `sourceId`（客户端暂未用，想按来源分组随时可用）；`source` 字段保留：单来源 = 那个频道，多来源 = 「音乐」。后台「音乐频道」页多了来源筛选和来源列（`/admin/music/tracks?sourceId=`）。已部署，客户端不用改。

- **消息页「花边新闻」入口改为「音乐」（Telegram 音乐频道同步，2026-09-19）**：三端消息「私聊」tab 顶部 AI 助手下方的入口换成「音乐 · 每日上新」，进独立音乐页（列表 + 底部常驻播放器：进度可点/拖、上一首/播放暂停/下一首、单曲结束自动下一首、封面旋转 + 播放动效）。**只保留最近 3 天**的曲目，过期连文件一起删。花边新闻/励志行的后端与页面代码保留，只是没入口了（励志行主页 tab 不受影响）。
  - **为什么要登录 Telegram 账号**：目标频道 `@wenan_DJ866` 发的是音频文件；`t.me/s/频道` 网页预览不提供文件下载，而且该频道关闭了预览（`/s/` 302 回频道页）。所以用 **MTProto 用户账号**（GramJS `telegram` 包）读取：只读频道、不发消息。
  - 后端：`src/telegram/telegram.service.ts`（`TelegramClientService`：单客户端，登录态 StringSession 存 `sys_setting.tg_session`，重启自动恢复；api_id/api_hash 优先 `sys_setting.tg_api_id/tg_api_hash` → 环境变量 `TG_API_ID/TG_API_HASH` → 内置 Telegram Desktop 公共值 2040）。`src/music/music.service.ts`（每 10 分钟同步：`getMessages(filter=InputMessagesFilterMusic)` → 过 3 天的跳过 → `downloadMedia` → `UploadService.putInternal('music')` 转存 MinIO → `music_track`；每轮每来源最多下 8 首，游标边下边推进；总量上限 200；封面取文档内嵌缩略图）。表 `music_source`（channel/title/subscribers/enabled/lastMsgId…）、`music_track`（sourceKey=tg:频道:消息id 去重）。`sys_setting.value` 放宽到 VarChar(2000)。
  - 接口：用户端 `GET /music?beforeId=` → `{source:{title,channel}, list:[{id,title,performer,duration,size,url,cover,postedAt,playCount}]}`（最近 3 天，每页 50）、`POST /music/:id/play` 计数。后台（AdminGuard）：`GET /admin/telegram/status`、`PUT /admin/telegram/api`、`POST /admin/telegram/send-code {phone}`、`POST /admin/telegram/sign-in {code,password?}`（回 `needPassword:true` 表示开了两步验证）、`POST /admin/telegram/logout`；`GET /admin/music/sources`、`GET /admin/music/sources/preview?channel=`（**解析频道**：标题/订阅数/简介/最近音频列表，不入库）、`POST /admin/music/sources`（保存前会解析，解析失败不保存；**换频道会把旧频道曲目和文件全删、游标归零**）、`DELETE /admin/music/sources/:id`、`POST /admin/music/sources/:id/sync`、`GET /admin/music/tracks`、`POST /admin/music/tracks/:id/delete`、`POST /admin/music/purge`。
  - 管理后台 `admin/src/views/MusicView.vue`「音乐频道」页：① Telegram 账号（手机号发码 → 输码/两步验证密码 → 登录；可改 api_id/api_hash）② 频道来源（填频道 → **必须先点「解析」核对标题和曲目** → 再保存；立即同步/编辑/删除）③ 已同步曲目（试听/删除/清理过期）。
  - 客户端：Web `pages/Music.tsx`（`/music`，`<audio>` + MediaSession 锁屏信息）；Android `ui/screen/MusicScreen.kt`（`MusicCenter` 单例 ExoPlayer，离开页面继续播；通话开始、动态视频出声时自动 pause；路由 `music`）；iOS `MusicViews.swift`（`MusicCenter` 单例 AVPlayer + MPNowPlayingInfo/远程控制，`Route.music`；CallManager/VoiceRoomManager 开始时 pause）。**Android/iOS 需重编**；后端、Web、管理后台已部署。
  - **待操作者完成**：后台「音乐频道」→ 用自己的 Telegram 手机号登录 → 频道填 `wenan_DJ866` → 解析核对 → 添加来源 → 立即同步（一首 80~120MB，下载几十秒到一两分钟，耐心等）。被 Telegram 限流（API_ID_PUBLISHED_FLOOD）时去 https://my.telegram.org 申请自己的 api_id/api_hash 填到后台。
  - **分享链接卡片没图（2026-09-21 修）**：原分享地址 `/#/music/share/:id` 是 hash 路由单页，微信 / QQ / Telegram 抓链接卡片只能拿到 index.html，没有标题和封面。改成后端出页：`src/share/share.controller.ts` `GET /api/share/music/:id`（nginx `location /s/` → `/api/share/`，分享出去的是 **`https://app.yyheart.com/s/music/:id`**），返回带 `og:title / og:description / og:image(封面)` + 隐藏 `<img>`（微信取页内首图）的极简 HTML，`meta refresh` + `location.replace` 立刻跳回 `/#/music/share/:id`；没封面就不出 og:image。三端 `shareLink` 都改成短链（Android `MusicCenter.shareLink`、iOS `MusicCenter.shareLink`、Web `pages/Music.tsx`）。落地页与列表的封面 `<img>` 加了 `onError` 隐藏（加载失败露出渐变音符，不留裂图）。**改 nginx 后要 `docker exec <nginx容器> nginx -s reload`**。
  - **「两个频道一直显示同步中」= api 容器被 OOM 反复杀（2026-09-22 修，严重）**：`docker-compose.yml` 给 api 的 `mem_limit: 512m`，音乐同步把一首 100MB+ 的曲子整个 `downloadMedia` 成 Buffer（GramJS 拼块时还有一份副本）→ node RSS 到 488MB 被 cgroup OOM 杀掉（`dmesg` 可见 `Memory cgroup out of memory: Killed process (node)`）→ 容器重启 → 30 秒后又同步到同一首 → 又被杀，**每 3 分钟死一次，整个 API 跟着一起断**。日志里只有一行 `Killed`。第二个来源 `YYPD6` 因此永远没同步完（lastSyncAt 为 NULL），而后台的 `syncing` 是全局一个标志，所以两个频道都亮「同步中」。修法：① `UploadService.putInternalFile()`（MinIO `fPutObject` 从文件流式传），音乐同步改成 `downloadMedia(msg, { outputFile: 临时文件 })` → 传完删；容器内存稳定在 ~100MB；② 服务器 compose 里 api `mem_limit` 512m → **1024m**（已改，备份 `docker-compose.yml.bak-*`；这个文件不在仓库里）；③ `syncing` 改成只标正在同步的那个来源（`syncingId`）。**排查这类问题先看 `docker inspect peiwan-api --format '{{.RestartCount}}'` 和 `dmesg | grep -i oom`。** 同样把整文件读进内存的还有 Gallery 视频（≤80MB，暂未改）。
  - **音乐来源可设检查间隔（同日）**：`music_source.interval_min`（默认 10，5~1440），tick 改成每分钟看一眼、`lastSyncAt + intervalMin` 到点的才同步。后台来源表新增「间隔」「下次检查」列（`nextSyncAt` 由接口算），「状态」列同步中的来源显示「同步中」，「上次同步」没同步过显示「还没同步过」。已部署。
  - Dockerfile 加了 `PRISMA_ENGINES_MIRROR=https://registry.npmmirror.com/-/binary/prisma`：这次构建时 build 阶段 prisma postinstall 直连 binaries.prisma.sh 卡死 20 分钟不超时，改走 npmmirror 后 3 分钟构完。
  - 后续想法（用户提过）：私密树洞的 Telegram 同步目前走网页预览，关闭预览的频道抓不到；可以同样改走这个已登录的客户端读取。

- **群分享（二维码/邀请码入群，可设密码）**：群信息右上角「分享」→ 面板展示群二维码（内容 `peiwan://group?code=8位邀请码`）+ 邀请码（点击复制）。群主/管理员可切换「无密码/有密码」并设置入群密码（普通成员只能看）。别人扫码或在消息页「+」→「加入群聊」输邀请码 → 查到群名/人数 → 有密码的群必须输对密码才能加入；已在群里则直接进群聊。
  - 后端：`chat_group` 表新增 `invite_code`（8 位，去易混淆字符，首次分享懒生成，唯一索引）、`join_password`（空=无密码）。接口：`GET /im/group/:id/share`（成员看分享信息，明文密码只回给群主/管理员）、`POST /im/group/:id/share`（群主/管理员设密码，传空串清除）、`GET /im/group/code/:code`（入群预检：群名/人数/是否要密码/是否已是成员+会话id）、`POST /im/group/join-by-code`（body: code+password，密码错回 400"入群密码错误"）。
  - Android：`GroupScreens.kt`（GroupShareDialog 二维码+复制+密码设置+保存相册/系统分享；JoinGroupScreen 输码/扫码，路由 `join-group`）；`QrUtil.kt` 加 `groupQrContent`/`parseGroupCode`；消息页「+」改成菜单（创建群聊/加入群聊）。扫码复用 zxing PortraitCaptureActivity。
  - iOS：`ChatViews.swift`（GroupShareSheet、JoinGroupView，Route.joinGroup）；`QrViews.swift` 加同名工具函数，扫码复用 QrScanView；消息页「+」改 Menu。
  - Web：`ChatRoom.tsx` GroupInfoSheet 右上「分享」→ GroupShareView（qrcode 库生成二维码）；`ChatList.tsx`「+」改下拉菜单 + JoinGroupSheet（Web 不支持扫码，仅输码）。
  - **群列表 + 群头像 + 界面调整（第二轮反馈）**：
    - 加入群聊页新增**群列表**（`GET /im/group/list`：全部正常群的名称/头像/人数/是否需密码/是否已加入），点「加入」直接入群（带密码的弹密码输入），已加入显示「进入」直达群聊。`POST /im/group/:id/join` 已补密码校验（原来能绕过密码）。
    - **群头像**：建群时可选传 `avatar`（三端建群页加了选图上传）；群信息页显示群头像，群主/管理员点头像可换（PUT /im/group/:id）。没设置时后端在 getGroup/会话列表/codeInfo/群列表统一**回退群主头像**。
    - **励志行多样化**：generateQuote 加随机主题池（每性别 10 个主题）+ 把最近 20 条已用句子塞进 prompt 禁止雷同 + temperature 1.3。已实测连续两次生成内容完全不同。
    - **群邀请面板重设计**（三端）：去掉整行大「保存设置」按钮，改为「无密码/有密码」胶囊 + 行内小「保存」按钮一排，密码输入框在下方，底部分割线 + 保存相册/分享图片/关闭一排。
  - **「我的」页扫一扫**：头像区右侧扫码图标（Android `MeScreen` 自绘 `ScanIcon`（`ui/Common.kt`）；iOS `MeView` systemImage `qrcode.viewfinder`；Web `Me.tsx` 内联 SVG + getUserMedia/jsQR 取景）。扫到群邀请码直接进加群流程（三端 JoinGroup 页/弹层均支持 `initialCode` 预填自动查询，Android 路由 `join-group?code=`，iOS `Route.joinGroup(String?)`）；扫到收款码提示去「积分转赠」使用。
  - **Android/iOS 需重编**；后端与 Web 已部署（schema 已 db push，路由已探活）。

- **官网下载介绍页重做**（`deploy/app/www/site/index.html`，纯静态单文件无依赖）：玫红黑主题对齐 App UI 规范。结构：吸顶导航（功能/下载/网页版）→ Hero（大标题+slogan+下载按钮组+纯 CSS 手机模型模拟「遇见」页）→ 6 功能卡（遇见/视频通话/动态广场/加密聊天/同城搭子/积分礼物）→ 三步开始 → 底部下载 CTA → footer（用户协议 `/agreement/user`、隐私政策 `/agreement/privacy`）。Android 下载指向 `/site/app/peiwan.apk`（**该目录尚未上传 APK，发版时需放置**），iOS 按钮弹"上架审核中"。响应式（860px 断点）。
  - **顺带修复 nginx**：`/site/`、`/admin/` 原来用 `alias` + `try_files` 一直 404（alias 下 try_files 的 `$uri` 带 location 前缀导致路径拼错）；因目录名与 URL 前缀一致，改为 `root /var/www/peiwan;` 即可（`deploy/app/nginx/peiwan.conf`，改后 `nginx -s reload`）。注意 nginx 容器映射宿主机 **20080** 端口，验证要 `curl 127.0.0.1:20080/site/`（宿主 80 是别的服务）。

- **消息页 AI 助手（免费问答）**：三端消息「私聊」tab 顶部固定入口（渐变 AI 圆头像 + "免费"角标），点进独立 AI 对话页（AI 左侧带 AI 头像气泡、自己右侧带自己头像、"正在思考…"占位、右上清空）。
  - 后端：`houduan/src/ai/`，表 `ai_message`（userId/role/content，按用户隔离），接口 `GET /ai/messages`（近 100 条）、`POST /ai/chat`（带近 20 条上下文调模型，同用户串行防并发刷）、`POST /ai/clear`。**OpenAI 兼容协议**，环境变量换模型：`AI_BASE_URL`（默认 https://api.deepseek.com/v1）、`AI_MODEL`（默认 deepseek-chat）、`AI_API_KEY`。
  - `AI_API_KEY` 已配（DeepSeek，在 `/opt/peiwan/deploy/app/.env`），已实测能正常回复；换 key/模型改 `.env` 后 `docker compose up -d api` 即生效。
  - 客户端：Android `AiChatScreen.kt`（路由 `aichat`）、iOS `AiChatView`（`ChatViews.swift`，Route.aiChat）、Web `/ai-chat`（`AiChat.tsx`）。**Android/iOS 需重编**；后端与 Web 已部署。
  - **AI 回复支持 HTML**：系统提示词允许 AI 在用户要攻略/页面时输出完整 HTML（```html 代码块，max_tokens 提到 4000、超时 150s）。三端客户端从回复里提取 HTML 块，气泡下显示「🌐 网页内容·点击打开预览」卡片：Web 全屏沙箱 iframe（srcDoc）、Android Dialog+WebView、iOS fullScreenCover+WKWebView（`AiHtmlPreviewSheet`/`HtmlWebView`）。

- **花边新闻（消息页入口）+ 励志行（主页 tab）**：
  - **花边新闻**：消息「私聊」tab 顶部 AI 助手下方固定入口（📰 渐变头像 + "每小时更新"角标）→ 列表页（下划线分隔样式，下拉刷新 + beforeId 翻页）→ **item 点击直接内部网页打开原文**（与 AI 网页预览共用组件，不解析正文）；只有 AI 兜底生成的文章（无 sourceUrl）才走解析式详情页。按性别分流：男看情侣共苦/励志/女企业家，女看农村女孩逆袭。
    - 后端 `houduan/src/news/`，表 `news_article`（audience 1=男 2=女，title/summary/tag/content/source/sourceUrl；采集文只存标题/摘要/链接，content=摘要）。**真采集**：每小时（启动 10s 后补跑，每 10 分钟检查到点）按关键词轮换从 **360 新闻搜索**（`news.so.com/ns?q=…`）抓一篇：标题黑名单（赌博引流词）+ 相关性词过滤 → 标题查重 → 每受众保留 200 篇自动清理。**采集彻底失败才兜底 AI 生成**（此时 content 存 AI 正文、无 sourceUrl）。源选型说明：Google/Bing RSS 在国内服务器不可达/被重定向到首页，360 实测稳定；搜狗微信可做备选。
    - 接口：`GET /news?beforeId=`（列表，按登录用户性别分流，每页 20）、`GET /news/:id`（详情，AI 兜底文用）。
  - **励志行**：主页顶部 tab「动态 | 遇见 | 励志行」。**一页一句、竖排、左右翻页**（书卷感：宋体竖排、玫红「」与竖线、日期 `2026 · 08 · 17`、页码；近末页自动 `beforeId` 加载）。男看"女生口吻鼓励打拼男生"，女看"情感励志"。表 `daily_quote`（audience+day 唯一），每日定时生成，接口 `GET /news/quotes?beforeId=`（历史列表，每页 30）、`GET /news/quote`（当天单条，保留备用）。客户端：Web `Plaza.tsx` QuoteSection（scroll-snap）/ Android `NewsScreens.kt` HorizontalPager / iOS `NewsViews.swift` TabView `.page`。
  - **管理端手动触发**（AdminGuard）：`POST /admin/news/crawl` 立即采集一轮（两性别，不看间隔，采不到走 AI 兜底）；`POST /admin/news/quote` 重新生成今天的励志语句（覆盖当天已有）。均已实测。
  - **共用网页预览组件**（AI 聊天 HTML 与新闻原文都走它）：Web `components/WebPreview.tsx`（html→沙箱 iframe srcDoc / url→iframe src，带"浏览器打开"逃生口，个别站点 X-Frame-Options 拒嵌时用）；Android `ui/Common.kt` 的 `WebPreviewDialog(html|url)`（WebView）；iOS `ChatViews.swift` 的 `WebPreviewSheet(html|url)` + `WebContentView`（WKWebView）。
  - **AI 聊天支持 HTML**：系统提示词允许 AI 在用户要攻略/页面时输出完整 HTML（```html 代码块，max_tokens 4000、超时 150s），三端从回复提取 HTML 块显示「🌐 网页内容·点击打开预览」卡片。
  - 客户端：Web `Plaza.tsx`（QuoteSection）+ `/news`、`/news/:id`（`NewsDetail.tsx`）；Android `NewsScreens.kt`（QuoteSection/NewsListScreen/NewsDetailScreen，路由 `news`、`news/{id}`）；iOS `NewsViews.swift`（QuotesSectionView/NewsListView/NewsDetailView，Route.newsList/newsDetail）。**Android/iOS 需重编**；后端与 Web 已部署，服务器已实测（采集入库、励志生成、手动触发接口）。

- **积分不足发消息无提示（bug 修复）**：后端本身正常（男→女每条消息扣 `price_config.msg_price_fen`，余额不足时 WS 回 `{op:'error',tempId,msg:'积分不足'}`，已用零余额男号实测确认）。问题在客户端两端：
  - Android：OkHttp WS 回调在后台线程，聊天页 listener 里 `Toast.show()` 无 Looper 直接抛异常且被 `WsClient` 的空 catch 吞掉（toast 没弹、乐观消息也没撤回）。修复：`WsClient.onMessage` 统一 `mainHandler.post` 切主线程分发（对齐 iOS 的 DispatchQueue.main），listener 逐个 `runCatching` 防单个异常影响其他。**需重编 Android**。
  - Web：`ChatRoomPage` 此前根本没处理 `error` 帧。已补：按 tempId 撤回 pending 消息 + 顶部黑底 toast（2 秒自动消失）。已部署。
  - iOS：本来就正常（主线程分发 + toast），无改动。

- **Web 聊天室布局对齐 iOS**（`web/src/pages/ChatRoom.tsx`）：顶栏只留 清空（+群聊的 群信息），礼物/通话入口移到底部「+」功能面板；底部输入区改微信式——圆角输入框 + 圆形「+」按钮呼出 4 列面板（相册/位置/语音通话/视频通话[仅男]/礼物，通话点击弹下载 App 引导），发送键仅有文字时出现；气泡昵称只显示对方的（自己的不显示）；图片消息点击全屏查看（黑底 mask，点击关闭）。已读/未读、发送中标记为 Web 特有功能，保留未动。

- **「我的」菜单调整**：编辑资料移到第一位；**实名认证入口删除，与搭子认证合并**——搭子认证申请时本地核验姓名+身份证号（查重），后台审核通过时把实名信息写入用户（`realName`/`idCard`，身份证被他人占用时只标记 isGuide），`realnameVerified` 徽章、女生发动态实名门槛逻辑不变（提示语改为"请先完成搭子认证"）。原 `/user/realname` 接口与实名页面保留未删，只是无入口。

- **照片墙**：编辑资料最多 8 张（`PUT /user/albums` 整组替换，存 `user_album` type=1），他人主页「关于我」展示 + 点开大图。三端（Android/iOS/Web）均已实现。**支持多选上传**（一次最多选剩余可加张数，超出自动截断），标题下提示"最多 8 张，已 X 张，还可选 Y 张"。
- **评分后留在女方主页**：男方视频挂断评分（提交或跳过）后自动进入该女生个人主页。Android 走 `CallManager.openUserHome` → MainActivity 导航 `u/{id}`；iOS 走 RootView fullScreenCover。
- **男方视频默认关画面**：男方发起/接通视频时自己画面默认关闭（仅禁用 track，通话中可手动开）。「我的」页新增开关"视频通话开启我的画面"（仅男方可见，存本地 SharedPreferences/UserDefaults `camDefaultOn`），女方无此设置且画面始终默认开。

- **「我的」头部与积分卡融合**：独立积分卡移除，积分余额做成头部渐变区内的玻璃质感行（白 5% 底 + 玫红描边，左侧余额大字、右侧冻结/明细），点击进钱包。三端一致。
- **「我的」关注/粉丝可点进列表**：复用后端 `GET /user/follows/list?type=following|fans`（此前无客户端入口）。三端新增列表页（头像+昵称+认证徽标+签名/城市，点击进他人主页）：Android `FollowListScreen`（路由 `follows/{type}`）、iOS `FollowListView`（Route.followList）、Web `/follows/:type`（`Follows.tsx`）。
- **动态卡片排版微调**：说明文字与图片/视频之间的间距统一为 8（原 Android/Web 为 10、iOS 为 0 贴死）；无文字时媒体与头部保持 10。三端 feed 卡片均已调整。
- **五维度展示词已委婉化**（字段名不变）：photo=真实度、obedience=配合度、legs=腿型、chest=曲线、skin=肤质。改动位置：三端主页评分方格 + 两端通话后评分弹框（原"腿长要你命"等已弃用）。
- **他人主页五维度评分面板**（已重设计，弃用进度条方案）：后端 `/user/:id` 的 `rating` 新增五维度均分（0-100）`photo/obedience/legs/chest/skin`（`callRating.aggregate` 实时聚合）。三端「关于我」在关注/粉丝下方展示：标题行「评分 ★★★★☆ 4.5 23次评价」（五星制 = 0-100÷20），下面 5 个维度按分从高到低排成 **3+2 等宽方格**（分值大字 + 维度名小字），**最高分那格玫红渐变填充白字**、其余白 6% 灰底——一眼看出最突出维度。有评分才显示；关注/粉丝数据行里不再重复放评分。
- **他人主页 hero 轮播自动播放**：三端头图（头像+照片墙）每 3.5 秒自动翻页循环（Android `LaunchedEffect`+`animateScrollToPage`、iOS `Timer.publish` 驱动 TabView selection、Web `setInterval`+`scrollTo smooth`），用户手动滑动时 Android 跳过本次自动翻页。
- **编辑资料页三端统一重做**：分组卡片布局（头像居中带"更换"角标+提示 → 基本信息卡[昵称/年纪/城市] → 视频价格卡[女生，含手续费提示] → 签名卡[80 字计数] → 照片墙卡[标题+右侧计数]），无边框行内输入。**年纪均为底部弹层选择**（18-70）：Android 自绘 snap 滚轮（`MiscScreens.kt` 的 `AgeWheel`）、iOS 原生 `.wheel` Picker sheet（取消/确定，`ProfileViews.swift`）、Web 网格弹层（原有）。iOS 保存按钮移到导航栏右上（对齐 Android/Web），城市改可手输+定位。
- **他人主页三端重做**（参考竞品截图）：顶部全屏 hero **轮播（头像+照片墙横滑，多图带页码圆点）**（左下叠接通率/视频价格）+ 圆角卡片（关于我/我的动态 tab + 关注胶囊）+ 底部操作栏（聊天/礼物圆钮 + 视频聊天大按钮两行文案：主标题+小字价格）。认证信息为简约行（玫红勾+文字，无背景卡）。
  - 文件：`android .../ui/screen/UserHomeScreen.kt`、`ios .../UserHomeView.swift`、`web src/pages/UserHome.tsx`。
- **认证信息卡**：徽章 chip（圆形 ✓ + 标签），平台认证 + 实名认证（仅女生显示）；后端 `/user/:id` 新增 `realnameVerified`（依据 idCard 是否绑定）。
- **「我的动态」媒体尺寸**：单图/视频大图（2/3 宽 3:4，视频带播放圆标）；2-3 图等分方格铺满整行。
- **iOS 子页面隐藏底栏**：底部 5 tab 是自定义栏（非系统 TabView），在 `Common.swift` 的 `withRoutes()` 给所有 push 路由挂 `HideTabBarModifier`（计数式），`MainTabView` 在 `TabBarVisibility.depth > 0` 时隐藏底栏。
- **通话评分系统**：视频通话结束后男方 5 维度评分（`CallRating` 表），均分缓存到 `User.ratingAvg/ratingCount`，影响广场排序。
- **视频按钮三态**：可打（显示价格）/ 通话中（橙）/ 离线（灰），广场卡片 + 他人主页 + 底部操作栏统一。
- **贡献榜**：积分明细页 tab，男看送花榜、女看金主榜（`GET /wallet/contrib-rank`）。

## 待验证（操作者手机上确认，需重编两端）

- [ ] 主页只剩「动态 | 遇见」两个板块；遇见板块顶栏无「视频」入口；「我的 → 关注动态」能看关注的人的动态。
- [ ] 主页「遇见」tab：四个子栏切换、卡片点开进主页、视频按钮可呼、亲密度排序与 ♥ 分数显示。
- [ ] 聊天/视频/点赞/评论后亲密度实时上涨（发起 +1、接受 +0.5，视频按分钟）。
- [ ] 编辑资料照片墙增删、保存后他人主页可见（两端 + Web）。
- [ ] 女生编辑资料改视频价格：手续费提示、低于手续费保存被拒。
- [ ] 男方视频呼出默认黑画面，我的页开关打开后恢复默认开。
- [ ] 男方评分（或跳过）后停留在女方个人主页。

- [ ] Android 绿色按钮包体感流畅度（isDebuggable=false 后 ART 全量优化）。
- [ ] iOS 进子页面底栏消失、返回恢复，多级跳转无闪现。
- [ ] 他人主页新版布局（hero 轮播、tab、底部操作栏、认证行、动态媒体大图）。
- [ ] 他人主页评分区：星级总分 + 3+2 五维度方格、最高分渐变高亮（有评分的女生主页显示）。
- [ ] 他人主页头图自动轮播（3.5 秒翻页，多图时生效）。
- [ ] 编辑资料新布局（三端）：年纪点开滚轮/弹层可选、分组卡片样式、保存正常；iOS 保存在导航栏右上。
- [ ] 通话一次后管理后台「通话日志」有数据、能下载。
- [ ] iOS 视频通话挂断后评分界面可正常点击（修复了 PassthroughWindow 在 idle 时穿透触摸导致评分界面点不动的问题）。

## 关键目录速查

| 位置 | 内容 |
|---|---|
| `houduan/` | NestJS 后端（Prisma + MySQL + Redis + MinIO），`src/call` 通话信令与日志，`src/admin` 管理接口 |
| `web/` | React 用户端（Vite），`src/pages/UserHome.tsx` 他人主页 |
| `admin/` | Vue 管理后台，`src/views/CallLogsView.vue` 通话日志模块 |
| `android/` | Kotlin + Compose，`rtc/CallManager.kt` 通话核心（含日志上报），`ui/screen/UserHomeScreen.kt` |
| `ios/PeiwanIos/` | SwiftUI，`CallManager.swift` 通话核心（含日志上报），`UserHomeView.swift`，`Common.swift`（路由/底栏隐藏） |
| `docs/` | `整体策划方案.md`、`UI设计规范.md`、本文档 |

## 长期注意事项

- 性别隔离全部走服务端强制（广场/搜索/资料页），客户端只是配合展示。
- 积分单位统一为「分」（100 分 = 1 积分），前端展示用 `fmtPoints`。
- 消息内容 AES-256-GCM 加密存储；聊天清空是双向删（单聊）/删自己（群聊）。
- Web 端不做音视频通话，提示下载 App。
- UI 规范：纯黑沉浸 + 玫红渐变（#FE2C55 → #FF6B81），无 emoji 图标，详见 `docs/UI设计规范.md`。
